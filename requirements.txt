# 依赖清单原则：**只列实际用到的东西**（没用过的包不写在这里，免得被当成"项目技术栈"）。
# 注：torchvision / pandas / tqdm 本项目代码未直接 import，但 ultralytics 运行时确实依赖它们
# （NMS 算子、结果解析、训练进度条），故保留。
#
# 核心框架
torch>=1.10.0
torchvision>=0.11.0
ultralytics>=8.0.0
numpy>=1.19.0
# OpenCV：**三个分发包共用同一个 site-packages/cv2/ 目录，最后安装者生效。**
# 本机实测（2026-09-16）：三者并存，实际生效的是 opencv-contrib-python 5.0.0.93
# （cv2.__version__ == "5.0.0"，而 pip show opencv-python 仍报 4.13.0.92）。
# 原因：mediapipe 依赖 opencv-contrib-python、albumentations 依赖 opencv-python-headless，
# 本文件原先只声明 opencv-python —— pip 会同时满足三方，但它们写的是同一份 cv2/。
# 故此处改为声明**真正必需**的那一个（mediapipe 要的 contrib 版）。注意：
# ⚠️ 绝不能让 opencv-python-headless 成为最后安装的那个：headless 没有
# imshow / waitKey / destroyAllWindows / getWindowProperty / WND_PROP_VISIBLE，
# 而 src/deploy/detect.py 用了它们，实时预览会直接崩，且报错指向 cv2.imshow 而非安装顺序。
# 护栏：tests/test_opencv_environment.py（被 headless 覆盖时它会红，不会 skip 掉）。
opencv-contrib-python>=4.5.0
mediapipe>=0.10.0
# GUI 调试面板为**可选项**：需要自行安装 PyQt5（本机未安装，GUI 面板从未运行过），
# 日常运行一律带 --no-gui。故不列入依赖清单 —— 清单里只放"实际用到"的东西。

# 数据增强与处理
# 需要 >=2.0：src/data/augment.py 使用 2.x 的 CoarseDropout 参数名。1.x 下参数名不同，
# 传 2.x 名字会被静默忽略（曾因此让预期"最多 8 个 ≤32px 洞"变成默认的"1~2 个 10~20% 洞"）。
albumentations>=2.0.0
pandas>=1.2.0
matplotlib>=3.3.0

# 评估与指标
# WIDER Face 官方分项评测（**可选**）。
# 项目实际汇报的指标不依赖它：src/eval/evaluate.py 用 ultralytics 自算 mAP，缺此包时把
# Easy/Medium/Hard 报为 null（不再用 mAP50 冒充）；分项分析由 analyze_errors.py 与
# conf_sweep.py 以"按尺寸分层"的方式覆盖。
# 仍列在此处，是因为将来若能安装，官方 Easy/Medium/Hard 指标值得补齐。
# 注意：安装需要网络，本机受限模式下不可达。
# ⚠️ 因此**默认注释掉** —— 它是可选包，留着会让 `pip install -r requirements.txt` 在这台机器上直接失败。
# widerface-evaluate>=0.1.0

# 模型导出与加速
# onnx：本项目代码不直接 import，但 torch.onnx.export 需要它（导出链路依赖，故保留）。
# onnx-simplifier 已移除 —— 全仓库从未 import（导出走 torch.onnx + onnxruntime 校验）。
onnx>=1.10.0
onnxruntime>=1.10.0

# 训练监控
tensorboard>=2.5.0

# 工具
tqdm>=4.60.0
PyYAML>=5.4.0
websocket-client>=1.8.0
