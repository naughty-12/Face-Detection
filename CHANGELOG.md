# 更新日志

本文件记录项目的重要变更。

## [未发布]

### 变更

- **项目文档重写：仓库收敛为 README 单一文档 + 三份旧工程文档归档**（2026-09-22）。
  - `README.md` **整份重写**（原 690 行 → 16 节）：概览与角色 → **§2 指标与口径基准**（§2.4 为四个速度口径，取代原先 §2.4/§2.5 互相重复的两张表）→ §3 架构与代码组织（吸收原《架构说明》的设计原则、依赖方向、决策与代价、扩展点）→ §4 环境与安装（吸收原《使用指南》的三个坑）→ §5 快速开始（并入原《使用指南》的常见问题表）→ §6–§9 数据 / 训练 / 评估 / 导出 → §10 实时表情捕捉管线 → §11 三条出口 → §12 工程规范与测试 → **§13 被否决的结论**（新增，集中 A/B 裁剪否决、通用增强接入否决、阈值结论、UDP 限频撤销、张量 MAE 判据改判）→ §14 已知限制（21 条，合并原差距台账中未关闭的项与"如果重做"优先级）→ §15 复现命令与证据索引 → §16 文档导航与许可。
  - **角色口径改为 A 并写进 README 开头**："项目由我主导，实现环节由 AI 辅助完成"（原文为"由一人独立完成的工程实践项目"）。
  - **归档**：`docs/架构说明.md`、`docs/使用指南.md`、`docs/项目现状与差距.md` **整份移入 `docs/archive/`** —— 正文保留，文首加"已被 README 取代"横幅（《使用指南》横幅另列出四类过时信息；《项目现状与差距》横幅列出已关闭 / 未关闭的 G 项）。`docs/archive/README.md` 增补三行列表与"当前文档在哪"，并把"本项目由一人独立完成"改为 A 口径。
  - **口径订正 7 处**：① README §2.4 速度表改用实测日志口径（含前后处理 **11.4 ms / 87.8 FPS**、ONNX FP32 27.7 ms / FP16 27.9 ms）并与 §2.5 去重；② README §10.1 `EMA(0.35)` → **0.55**（`--bbox-alpha` 默认值覆盖类默认值）；③ `面试讲解提纲.md` §一/§四.2 自相矛盾的 88–147 FPS 与 145–147 FPS → **四口径表**，§八 FPS 问答同步；④ `面试讲解提纲.md` "六个模块全部由我一人独立完成" → A 口径；⑤ `简历项目经历.md` 把 10.31 ms 由"含前后处理"改标为**裸网络前向**、并加文首口径横幅；⑥ `AI协作复盘.md` 单帧数字补口径；⑦ `面试问答弹药库.md` 关于"README §2.4 与 §2.5 印同一组数"的括注改为已订正。
  - **顺带更新**：`.dsh-dev/conventions.md` 测试条目（155 用例 / unittest / 未覆盖区）、`MEMORY.md` 索引（指向归档后的路径）、`docs/模拟面试项目全解.md` §13.2 文档地图与 §13.3 冲突表（改为"已订正的 7 处 + 仍未订正 2 处"）、`docs/模拟面试项目全解.md` 与 `README.md` 的交叉引用路径。
  - **证据**：`python -m unittest discover -s tests -t .` → **Ran 155 tests / OK**，exit code 0（本轮实跑）。
  - **提交与推送**：本轮改动最初按要求留在工作区未提交；后经使用者指示，于 **`3af1a41`**（`git add -u`，**只提交已跟踪文件**）一次性入库，并推送到 gitee `origin/main` 与 GitHub `main`。`docs/`、`.claude/` 与各 app 的 README **仍未跟踪**（沿用 2026-09-16「只发布 README」的决策）。此前误提交的一次（`a936e5d`）已用 `git reset --mixed HEAD~1` 撤回，文件内容一字未动。

- **新增自包含答辩底稿 `docs/模拟面试项目全解.md`**（2026-09-22）。
  - **起因与结论**：模拟面试需要一个文件讲清整个项目。核查后结论是**原有四份面试材料事实齐全但不足以直接应对** ——
    ① 口径分散且互相矛盾（同一个数字在三份文件里有三种说法）；② 弹药库 58 KB、内部大量 `§四.1` 式交叉引用，
    现场翻不到答案；③ 提纲是"节奏与讲稿"、弹药库是"分板块详答"、简历稿是"措辞与筛子"，
    **没有一份是"整个项目是什么"的全景叙述**，而模拟面试第一问就是这个。因此另写一份**自包含全景稿**（不依赖其他文件）。
  - **内容**：速览卡 / 三档自我介绍（含 A 口径的 AI 协作交底说法）/ 全链路全景 / 架构与运行时线程模型 /
    数据与训练 / **指标与口径基准表**（四个速度口径 + 尺寸分层 + 阈值扫描 + 导出变体）/ 客户端·游戏客户端岗最该讲的七条 /
    约 40 条高频问答 / 16 条已知不足 / 演示脚本与证据索引 / **红线清单（14 类不能说的表述）** / 文档地图与待订正项。
  - **顺带登记 7 处口径冲突**（新稿 §13.3，**源文档本轮未改，以新稿 §6 为唯一口径基准**）：
    ① `面试讲解提纲.md` §一"含前后处理 88–147 FPS"与 §四.2"145–147 FPS 是裸前向"自相矛盾；
    ② `README.md` §2.4 标称"含前后处理"却印着裸前向的 10.31 ms/97.0 FPS（§2.5 与之完全重复），
    日志实测含前后处理为 **11.4 ms/87.8 FPS**（`artifacts/logs/export_variant_comparison.log`）、ONNX 27.7 ms/36.1 FPS；
    ③ `README.md` §10.1 把 FaceBoxFilter 写成 `EMA(0.35)`，**实际生效 0.55**（`--bbox-alpha` 默认值覆盖类默认值）；
    ④ `面试讲解提纲.md` §八"六个模块全部由我一人独立完成"与 **A 口径**（决策者 + AI 辅助实现，面试主动交底）冲突；
    ⑤ `架构说明.md` §六/§七 仍写"无单元测试 / 56 个用例""标注解析有 4 份重复实现"，均已过时
    （现 **155 个用例**，解析已收敛为 `src/data/wider_annotations.py` 一份）；
    ⑥ `.dsh-dev/conventions.md` L51"`tests/` 为空、无测试框架"已过时；
    ⑦ `README.md` §2.4 的 ONNX 吞吐（24.80 ms/40.3 FPS）与 §2.5 重复，含前后处理实测为 27.7 ms/36.1 FPS。
  - `README.md` §16 增加一行指向该底稿（`docs/` 按 2026-09-16 决策未纳入版本库，本节仅作指向）。
  - **证据**：`python -m unittest discover -s tests -t .` 本轮实跑 **Ran 155 tests / OK**（0.235 s）。

- **口径订正 + 台账新增：OpenCV 三包共存的"未重装"理由不成立**（2026-09-22）。
  - 原文写"环境当前验证可用，**重装会把它变成未验证**"—— **该理由不成立**（验证是可重复动作，
    护栏 **0.1 s** 即可重跑），且把一条**未根治**的脆弱性标成了 ✅。三处（本文件、`docs/皮套联调问题与解决.md` 1.1、
    `docs/AI协作复盘.md` 8.1）订正为"**处置已完成 + ⚠️ 未根治**"；未重装的理由改为可核对的两条：
    **收益为零**（重装不改变"三个包都装着、生效者由安装顺序决定"的结构）+ **风险非零**（顺序不受控；
    受限 shell 的 pip 会再生成一份用户目录副本 = 第四份）。
  - `.dsh-dev/mistakes.md` 新增 **失误 9**：把"当时的权衡"写成"原则"、把未根治项标成已完成。
  - `.dsh-dev/decisions.md` 新增一条：**根治路径（未做）＝把只用于可视化与一次已否决实验的第三方增强库
    从运行环境拆出**。
  - 新增 `docs/AI协作复盘.md`（**B 口径**：显式提 AI 协作的复盘稿 —— 四段式 + 缩写版 + 一句话版 +
    单点问题简述与追问；与 A 口径的客户端版简历分开，不混投）。

- **对外表述定稿 + 仓库收敛为"只发布 README"**（2026-09-16）。
  - **口径决策**：采用 **A（不显式提 AI）**（使用者决策）—— 简历与对外材料只写决策与验收，
    面试主动交底；**项目名点名 VTube Studio**；**不写 Unity / C#**（该项目先不接 Unity）。
  - `docs/简历项目经历.md`：新增 **§3.5 最终版（四段式：项目描述 / 技术栈 / 技术特点介绍 / 全流程）**，
    并附 **§3.5.1「旧稿必须改的 14 处」**对照表（逐条给仓库证据）；§3.4 标注被 §3.5 取代
    （其"保留 Vibe Coding 定位"一句按 A 口径作废）；§1/§3.1/§3.2/§3.3/§3.4/§7/§8/§9/§10 的用例数、JD 对照、
    禁止清单、决策清单同步到今天的事实（含新增决策 13–16）。
  - **仓库侧**：三次提交 —— 桥接联调修复 + 稳定性采样器（`feat`）、VMC/Unity 出口代码（`feat`）、
    **说明文档撤出仓库 + README 校正**（`chore`）。撤出用 `git rm --cached`，**本地文件全部保留**
    （`MEMORY.md`、`CHANGELOG.md`、`docs/`、`.dsh-dev/`、`.claude/CLAUDE.md`、各 app README）。
    现在提交树里只剩 `README.md` 一个说明文档。⚠️ **历史里仍有这些文档**（未重写历史）。
  - **README 校正为实测状态**：测试数 56 → **155**；OpenCV 4.13 → **5.0**（`cv2.__version__` 实测）；
    **删除 PyQt5**（本机未安装、面板从未运行）；新增 **VTS 实测状态表**（端到端、599.7 s 稳定性、
    29.3 帧/秒 = 摄像头硬上限）与两项已知缺口（端到端延迟未实测、无演示视频）；去掉指向已撤出文档的死链。
  - **待办（等使用者发话）**：① 上传全部说明文档；② 是否补 LICENSE（仓库当前没有）；
    ③ `artifacts/diag/` 的证据文件（含 599.7 s 原始 CSV）是提交还是忽略；④ 是否重写历史以彻底移除文档。

- **第 5 条验收的判据修正 + 稳定性采样器入库（`sample_vts_params.py`）**（2026-09-16）。
  起因见 [`.dsh-dev/mistakes.md` 失误 8](.dsh-dev/mistakes.md)：那次 7 分钟采样的脚本**没进仓库、原始数据没落盘**，
  文档却已把第 7 条的证据写成"数字证据包"；而那个脚本**结论行的判据本身是错的**
  （把"桥接还没启动"当成了掉线）。
  - **先实测语义、再写判据**：`connectedPlugins` 来自 `StatisticsRequest`，含义是
    **VTS 当前活着的 API 会话数（含采样器自己）**。在桥接在线时实测：
    **2 个探针 + 桥接 = 3**、**1 个探针 + 桥接 = 2**、**只有采样器 = 1**
    （同响应里 `allowedPlugins` 恒 = 1，因为复用同一插件身份）。
    → **`plugins == 1` 不是掉线**，只是桥接还没起来；判据必须**只从"连上那一刻"算起**。
  - 同一轮实测确认两件事：读参数用 `ParameterValueRequest` 且键名是 **`name`**
    （传 `id` → `APIError 500 "You have to provide a parameter name."`），且**一次只返回一个参数**；
    采样器用桥接的插件身份 + 已缓存 token **不会二次弹授权窗**。
  - 新增 `apps/vtube_bridge/sample_vts_params.py`（只读观察者，默认采 **600 s**，要求 300 s
    —— 留 5 分钟余量，免得又"正好卡线"）：连续时长取**最长的一段**（断线/采样中断要重新计）；
    每个完整 30 s 窗口至少一个参数在变化；`uptime` 回退判 VTS 重启；**原始 CSV 边采边写、每行 flush**
    （半路崩掉也留下已采数据）；输出 CSV + JSON 结论 + 报告三件套，退出码 0 = 通过 / 2 = 未通过。
  - 顺手修掉一个真实缺陷：本机控制台代码页是 **65001(UTF-8)** 而 Python 的 stdout 是 **gbk**
    → 中文报告全是方块；现在按代码页自动切换（实测 `--help` 与运行日志中文正常显示）。
  - TDD：`tests/test_vts_stability_sampler.py` **28 个**（含"启动前的窗口不算掉线"那个 bug 的回归、
    VTS 重启、采样中断、参数冻结、CSV 边写边落盘、浮点 epsilon 边界、请求失败≠掉线）
    → 全量 **155 / OK**。
  - **两次 10 分钟实跑构成对照（这就是判据存在的理由）**：
    | 运行 | 连接侧 | 参数侧 | 判定 |
    |:---|:---|:---|:---|
    | ① 15:08 前那次（**人不在镜头前**） | 连续 599.9 s，掉线/采样失败/中断/VTS 重启**全 0** | **t≈102 s 起参数全部回落到 VTS 默认值**（`EyeOpenLeft=1`、其余 0 = 桥接注入 `faceFound:false`）→ 后 480 s 的 15 个窗口全 BAD | ❌ **FAIL（4/19）** |
    | ② **人入镜那次**（15:08:13–15:18:13） | 连续 **599.7 s**，异常计数**全 0** | **19/19 个 30 s 窗口**都有参数在变化（`FaceAngleX` 窗口跨度最大 **30.36**、`MouthOpen` 0.37、`EyeLeftX` 0.43、`EyeOpenLeft` 1.00） | ✅ **PASS（19/19）** |
    → **只看 VTS 日志 / 连接状态会把①当成"稳定运行 10 分钟"**，而实际上那 8 分钟一个参数都没被驱动。
  - 证据落盘（`artifacts/diag/vts_stability_20260916-150813.{csv,json,txt}`），并**独立重算复核过**：
    2090 行、`connectedPlugins` 每行都是 2、`allowedPlugins` 恒 1、错误行 0、最大采样间隔 **0.313 s**、
    `uptime` 单调递增 **+599.4 s**（VTS 未重启）、逐窗口跨度与工具结论完全一致。
  - 顺带实测到"**离开画面再回来仍能跟随**"（验收第 5 条要求的行为）：15:03:34–15:03:54 采到的是
    VTS 默认值（无人入镜），15:07:57 再采已恢复正常（`MouthOpen` 0.223→0.258、`FaceAngleX` −0.42→2.27）。

- **连续稳定性验收实测通过 + 演示视频按使用者决定不做**（2026-09-16）。7 分钟采样（0.15 s 间隔）：
  桥接连上后 **10/10 个 30 s 窗口参数均有变化**（`FaceAngleX` 窗口跨度 14.7–45.7、
  `MouthOpen` 0–0.56、`EyeOpenLeft` 0–1.00），**`connectedPlugins` 全程 = 2、无掉线**，
  连续 **≈5.0 分钟**（**正好卡在 5 分钟线上**，要留余量建议再跑一次 10 分钟）。
  - ⚠️ **采样脚本的结论行有 bug（如实记录）**：它把"桥接启动前"的 3 个窗口也算成了掉线，
    于是打印"未达 5 分钟或出现掉线，需重测"。**人工核对数据后判定为通过** ——
    掉线只发生在桥接尚未启动的前 1.5 分钟。正确判据应**只统计桥接连上之后的窗口**。
  - **演示视频不做**（使用者决定）→ `docs/皮套联调问题与解决.md` 第 8 节第 7 条改为
    "证据 = **数字证据包**（采样记录 + VTS 日志 + `artifacts/diag/` + 127 个回归测试）"。
  - **口径**：第 1–5 条有证据、第 6 条仅帧率有口径（**端到端延迟未实测**）、第 7 条无视频
    → **不得写"7 条验收全部通过"**；可写"**端到端跑通 + 连续 ≈5 分钟稳定性实测**"。

- **线 A 收尾：文档状态同步为「端到端已跑通 + 7 条验收逐条标注」**（2026-09-16）。
  - `docs/plans/2026-09-15-皮套落地三线.md`：线 A 状态由"发送端已验证 / 接收端未联调"
    改为 **★ 已端到端跑通**（附证据、关键数字与**仍缺的两项验收**）。
  - `docs/皮套联调问题与解决.md`：新增**第 8 节「7 条验收逐条标注」**
    （1–4 使用者目视确认 ✅ / 5 测量中 ⏳ / 6 仅帧率部分 ⚠️ / 7 演示视频未做 ❌）；
    回归清单改为**逐文件实跑计数**（新增 **71 个 / 全量 127**），修掉表里过期的"36 / 92"。
  - `apps/vtube_bridge/README.md`：已知限制改为 **★ 已跑通**，写明仍缺的第 5、7 项，
    并要求"端到端延迟未实测"不得给具体毫秒数。
  - `MEMORY.md`：新增「线 A 状态（皮套）」关键事实行。
  - **口径红线**：第 5、7 条补齐前，**对外表述不得写"验收通过"**。

- **实测结论：本机内置摄像头硬上限 30 帧/秒，采集参数改不动**（2026-09-16，`probe_camera.py`
  跑满 12 组组合，每组预热 10 帧后连读 60 帧）：

  | 组合 | 驱动实际接受 | read 均值 | 实测帧率 |
  |:---|:---|:---|:---|
  | msmf 默认（**最优**） | 640x480 @30 | 33.32 ms | 30.0 |
  | dshow 默认 | 640x480 @-1 `[YUY2]` | 33.33 ms | 30.0 |
  | msmf 640x480 **@60** | **@30（请求被忽略）** | 33.49 ms | 29.9 |
  | msmf / dshow 640x480 **MJPG** | **仍是 `YUY2`（请求被忽略）** | 33.6–35.4 ms | 28.3–29.8 |
  | msmf 1280x720 @30 / @60 | @30 | 33.6 ms | 29.8 |
  | dshow 640x480 @60 MJPG | 驱动**自称** @60 | 33.60 ms | **29.8（谎报）** |

  **全部 ≈ 33.3 ms = 1000/30，即摄像头的帧间隔。** 推论：
  - 之前 summary 里的 `read=15.8 ms` **不是可优化的开销**，而是"帧周期 33.3 ms − 算力 16.2 ms"
    的**等待时间**（15.8 + 7.8 + 8.4 = 32.0 ≈ 33.3 ✓）；管线只是在等摄像头。
  - `--imgsz`、`--yolo-every`、`--camera-*` **都提高不了这台机器的帧率**；29.3 帧/秒已贴着硬件上限。
  - 想超过 30 帧/秒**只能换摄像头**（管线自身算力约 16 ms ≈ 59 帧/秒，有余量能吃掉）。
  - 顺带发现：**驱动会谎报帧率** —— dshow 请求 @60 时 `CAP_PROP_FPS` 读回 60，实测仍 29.8。
    这正是探针必须同时打印「实际」列的原因（只信请求值会得出错误结论）。
  - **保留** `--camera-*` 参数与 `probe_camera.py`（换摄像头/换机器时用得上），**本机无需设置**。

### 新增

- **摄像头采集参数（`--camera-backend/width/height/fps/fourcc`）+ 基准工具 `probe_camera.py`**
  （2026-09-16）。起因：使用者摄像头路径 summary 为
  `read=15.8  yolo=7.8  mediapipe=8.4  total=32.6 ms → 29.3 帧/秒` —— **read 占近一半帧时间**，
  而管线自身只需约 17 ms（≈59 帧/秒 的算力），也就是**在等一个约 30 帧/秒的摄像头**。
  `--imgsz`（320/480/640 实测无差别）与 `--yolo-every`（跳帧只多出空闲）都动不了这一段，
  只有换采集方式才行，因此：
  - 新增 `apps/vtube_bridge/probe_camera.py`：对 12 组「后端 × 分辨率 × 帧率 × FOURCC」逐个
    预热 10 帧后连读 60 帧，报告 read 的**均值 / p95 / 实际帧率**，并读回驱动**实际接受**的参数
    （很多摄像头会静默忽略请求）；输出到控制台 + `artifacts/diag/camera_read_report.txt`。
  - 桥接新增五个采集参数，启动日志**同时打印请求值与实际值**
    （`Capture size: 720x1280 @30 fps` / `Camera request: backend=msmf 640x480 @60 MJPG`），
    避免"设了没用"看不出来。
  - 两个实现要点：① **先设 FOURCC 再设分辨率**（部分驱动改分辨率时会重置像素格式）；
    ② 摄像头参数**只对摄像头序号生效** —— `--camera-backend msmf` 配视频文件会直接失败
    （MSMF 打不开文件），故文件源一律跳过这些设置（本地冒烟 30 秒内踩到的坑）。
  - 非法值**提前失败**：非法 FOURCC 在 `parse_args` 即报错（否则要等模型加载完约 20 秒才炸）。
  - TDD：`tests/test_camera_capture_settings.py`（12 个测试，用假 cv2 验证后端传递、
    属性设置**顺序**、打不开时不设置、文件源跳过）。全量 **121 / OK**。

- **`--yolo-every N`：隔帧跑人脸检测，其余帧复用上一次的框**（2026-09-16）。实测（同一段
  720×1280 视频、`--sink unity` 指向死端口，240 帧全处理、无异常）：

  | 设置 | 管线帧率 | 单帧 total |
  |:---|:---|:---|
  | `--yolo-every 1`（默认） | **37.9 帧/秒** | 22.3 ms |
  | `--yolo-every 2` | **49.2 帧/秒**（+30%） | 17.6 ms |
  | `--yolo-every 3` | **52.0 帧/秒**（+37%） | 15.9 ms |

  - **⚠️ 对摄像头路径几乎没有收益（重要口径）**：使用者实跑 summary 为
    `read=15.8  yolo=7.8  mediapipe=8.4  total=32.6 ms → 29.3 帧/秒` —— **read 才是大头**：
    15.8 ms 是在**等一个约 30 帧/秒的摄像头**。管线自身只需约 17 ms（≈59 帧/秒 的算力），
    跳帧只是多出空闲时间，**不能突破摄像头帧率上限**。要提摄像头路径的帧率，得从
    **摄像头格式/帧率**入手（见 `docs/皮套联调问题与解决.md` 3.8）。
  - **实现要点（踩过的坑）**：跳帧时必须**复用上一次的框**，不能给 `FaceBoxFilter` 传 `None` ——
    传 `None` 会被当成"丢脸"，跳帧一多就进入 `held` 状态，而 `held` 会跳过 MediaPipe，
    于是表情回落到默认值、**眼睛每帧弹开**。
  - 按 TDD 落地：纯函数 `should_run_detection()` + `tests/test_frame_skipping.py`（5 个测试，
    含"`every<=0` 必须退化为每帧都跑，绝不退化成从不跑"与"第 0 帧一定跑"）。
  - 全量测试 **104 / OK**。

- **结束时打印分段耗时（`format_stage_timings`）**：桥接本来就在 `record_timing` 里记录各段耗时
  （供可选 GUI HUD 用），但 `--no-gui` 下一行都不打印，"慢在哪一段"只能靠猜 —— 实测同一段视频
  从文档记的 **40.2 帧/秒** 变成过 **23.0 帧/秒**，而把 `--imgsz` 从 640 降到 320 毫无变化。
  现在 summary 会多出一行，实测样例：
  ```
  [summary] 处理帧数 240，耗时 6.3 s → 管线 38.0 帧/秒
  [summary] 分段耗时（均值，ms）：read=1.5  yolo=9.5  mediapipe=11.0  vts=0.1  debug=0.0  total=22.8
  ```
  据此可判定：**YOLO 只占 9.5 ms、MediaPipe 固定 11.0 ms**，所以调 `--imgsz` 触及不到瓶颈；
  另查明此前那次 "23.0 帧/秒" 是**两个管线实例同时抢 GPU 造成的测量污染**，干净条件下为 **38.0 帧/秒**。
  按 TDD 落地：新增 `tests/test_stage_timing_summary.py`（4 个测试）。
  **测试写法注意**：该文件**刻意不用 `skipIf`** —— 函数缺失必须是**失败**而不是 skip
  （第一版写成 skipIf，实测被跳过 4 条，等于没有护栏）。

- **VTS 侧链路首次跑通并留下实测证据（2026-09-16）**：用 VTube Studio Public API 走通
  "授权 → 列模型 → 加载主模型 → 参数核对 → 注入回读"，数据如下：
  - `AvailableModelsRequest` 列出 **5 个主模型**；`ModelLoadRequest` 加载 hiyori
    （`modelID 5b786375d675445cb5301bb032dc333c`）→ `CurrentModelRequest` 返回
    `modelLoaded=true, modelName=hiyori`；此前被误拖成挂件的 `hiyori_vts` 由 `ItemUnloadRequest` 卸掉。
  - `InputParameterListRequest` → 该模型 **127 个输入参数**，桥接要用的 **16 个全部存在
    （MISSING: NONE）** —— 文档原先担心的"2D 模型参数集不全导致驱动不了"，在这条路上**不成立**。
  - `InjectParameterDataRequest` → `ParameterValueRequest` 回读：`MouthOpen` 0.77→0.7699999809265137、
    `FaceAngleX` 22.5→22.5、`EyeOpenLeft` 0.25→0.25、`EyeLeftX` 0.6→0.6000000238418579、
    `MouthSmile` 0.9→0.8999999761581421，**全部 MATCH**，随后已复位到中性。
  - **口径**：以上均为 **VTS 侧（接收端）** 的证据。**"摄像头 → 桥接本体 → 注入"这一段仍未运行过**，
    "皮套跟随摄像头"尚不成立。

### 修复

### 修复

- **"眯眼很容易闭上"：按实测把眨眼阈值移进「眨眼谷底」与「眯眼水平」之间的空隙**（2026-09-16）。
  上一版用纯深度吸附（`snap_below=0.5`）把眯眼一起吞了。用
  `--blink-snap-below 0 --blink-deep-below 0` **关掉吸附**后，从 VTS 回读值反推 raw，
  240 秒采样得到三条分布：

  | 动作 | raw（眼睛真实张开度，0=全闭、1=全睁） |
  |:---|:---|
  | 眨眼谷底 / 真闭眼 | **0.300 – 0.365** |
  | 眯眼水平 | **0.410 – 0.550** |
  | 睁眼 | 0.63 – 0.97 |

  两者之间有条约 **0.04 的空隙** → 阈值设进空隙即可，**不必再靠"时长"去赌**：
  - `--blink-snap-below 0.40`（眨眼照旧闭实，眯眼永不吸附）
  - `--blink-deep-below 0.33`（真闭眼不限时长）
  - `--blink-squint-frames 6`（只兜底"眯眼过程中偶发下探"）

  实测行为（默认档）：`raw 0.30 -> 0.000`（全闭）、`raw 0.36 -> 0.000`（闭实）、
  `raw 0.45 -> 0.217–0.368`（**半闭、不误闭**）、`raw 0.52 -> 0.319–0.449`（半闭）、
  `raw 0.30 保持 30 帧 -> 0.000`（真闭眼不放开）、`raw 0.95 -> 0.94`（睁眼不受影响）。
  回归测试：`tests/test_blink_stabiliser.py` 增至 **17 个**（新增"持续眯眼不被吞掉""深闭合不限时长"
  "眯眼放开后下一次眨眼仍能闭"），并把"0.40 应当吸合"这类**按旧阈值写的用例改成按新阈值**。
  全量 **127 / OK**。

- **眨眼"不抖但不实"：改用带迟滞的"闭眼吸附"，而不是抬高校准下限**（2026-09-16）。
  150 秒采样（从 VTS 回读、按当时校准反推 raw）测得**自然快眨的谷底落在 raw 0.35–0.49**，
  而校准下限 0.30 只把 0.30 以下压成闭合 → 大多数眨眼留下 5–25% 的缝（即"稍微一点点的睁开"）。
  - **为什么不抬高校准下限**：映射是线性的，把跨度从 0.69 压到 0.47 会让**睁眼抖动放大 1.5 倍**，
    而"抖"正是上一轮的投诉。所以不缩放，改为**替换**：平滑值在**下降过程中**低于
    `snap_below=0.5` 就直接输出 0；要重新睁开必须让**原始目标**升过 `unsnap_above=0.7`
    （**迟滞**是关键，否则值在阈值附近会反复闪）。
  - 实测（同一套校准 min=0.30）：

    | 谷底 raw | 修复前发给 VTS | 修复后 |
    |:---|:---|:---|
    | 0.30 | 0.000 ✅ | 0.000 |
    | 0.40 | 0.145 ❌ 有缝 | **0.000** |
    | 0.45 | 0.217 ❌ 有缝 | **0.000** |
    | 0.55 | 0.362（半闭） | 0.368（**保留**半闭） |
    | 0.75 | 0.652（半睁） | 0.661（**保留**半睁） |

    重开过程平滑（`0.44 → 0.68 → 0.81`），无跳变。
  - TDD：`tests/test_blink_stabiliser.py` 扩到 **12 个测试**（新增 5 条吸附/迟滞用例；
    其中两条原有用例显式设 `snap_below=0` 以隔离非对称 alpha 本身）。全量 **109 / OK**。

- **眨眼"更抖"且"闭不上"：改用非对称平滑 + 收紧校准下限**（2026-09-16）。使用者对两版的反馈
  **互相矛盾但都成立**：`--expression-alpha 0.65` → "更抖"；默认 0.45 → "大多数时候闭眼闭不上，
  有稍微一点点的睁开"。原因是一个对称 alpha 不可能又快又慢，而真人眨眼本身不对称（闭合 ~50 ms、
  睁开略慢）。因此：
  - 新增 `BlinkStabiliser`：**只在确实在闭时**（目标明显低于当前值，差 > 0.25）用快 α=0.8，
    其余时候用 0.45 —— 睁眼状态下的眨眼噪声被温和掉，真眨眼仍"啪"地闭上；
    眼睛两键改走它，其余表情仍走原 `ParameterFilter`（`tests/test_blink_stabiliser.py`，7 个测试，
    **不用 `skipIf`**：类缺失必须失败）。
  - 校准下限由 `0.25/0.26` 收紧到 **`0.30/0.32`**（实测你的自然快眨谷底落在 raw 0.30–0.36，
    旧下限会让它留下 4–9% 的缝隙，即"稍微一点点的睁开"）。实测映射：`raw 0.30 -> 0.000`、
    `raw 0.45 -> 0.22`（半闭仍可分辨）。
  - **全量测试 99 / OK**。观感待复验。

- **歪头幅度再降 23%**（2026-09-16，使用者第二次反馈"再调小一点点"）。`FaceAngleZ` 源范围
  ±50° → **±65°**（增益 0.46）：`raw 30° -> 13.85°`（上一版 18°）、`raw 44° -> 20.3°`。
  若"卡顿"仍在，优先怀疑**发送率**（默认 `--send-fps 30` 会把更新率压到 30 Hz）而非幅度。

- **眨眼左右相反：`estimate_expressions` 的两只眼接反了**（2026-09-16，使用者目视确认
  "**眨左眼反馈的是右眼眨**"）。MediaPipe 的 blendshape 名按"被拍者自身"命名（ARKit 习惯），
  直接对应（使用者的左眼 → `EyeOpenLeft` → hiyori 的 `ParamEyeLOpen` = 模型自己的左眼）
  在解剖学上一致，但使用者看到的是**另一侧的眼在眨**；期望是**镜像式**（面对你的形象应闭上
  与你同侧的那只眼），故交换两处取值。
  按 TDD 落地：新增 `tests/test_eye_open_sides.py`（5 个测试：单眼眨、双眼眨、部分眨不贴端点），
  RED 实测 3 个失败（`1.0 != 0.0 : the user's left wink must drive EyeOpenRight (mirror-like)`）
  → 修复后 **Ran 88 tests / OK**。

- **歪头幅度过大：`FaceAngleZ` 的增益降到 0.6**（2026-09-16，使用者反馈"歪头的时候参数调的过大，
  看起来会卡顿"）。原先源范围 ±30° → VTS ±30° 是 1:1 增益；实测使用者歪头输入范围约 −44°..+28°，
  因此把源范围放宽到 **±50°**（增益 0.6）：`raw 30° -> 18°`、`raw 44° -> 26.4°`，幅度降 40%
  且不会削顶。改动在 `apps/vtube_bridge/config/tracking_calibration.json`。

- **左右摇头方向相反：`estimate_angles` 的 yaw 未取负**（2026-09-16，使用者目视确认）。
  `FaceAngleX = +yaw` → 改为 `-yaw`；`FaceAngleY`（点头）与 `FaceAngleZ`（歪头）未被反馈为相反，
  保持原符号。这三个符号**只能由目视确定**，改动前请先读
  `tests/test_head_rotation_direction.py` 的说明。
  按 TDD 落地：新增 `tests/test_head_rotation_direction.py`（5 个测试，用合成旋转矩阵把
  "矩阵 +10° → 输出 −10°"钉死，并锁住点头/歪头原符号与三轴独立性），RED 实测
  `AssertionError: 10.000000363380625 != -10.0 ... FaceAngleX must negate the matrix yaw
  (user-verified direction)` → 修复后 **Ran 83 tests / OK**。

- **订正"视线方向"这条结论（此条前后判断了三次，最终口径如下）**：视线符号的两次目视反馈
  **互相矛盾** —— 原代码取反时被报"眼睛相反"，去掉取反后又被报"眼睛相反"；而使用者随后补充
  "**左右摇头方向也是反的**"。看某一侧时头会跟着转，**头方向反**足以让整个"视线"看起来都是反的，
  因此判定真凶是**头**（见上一条）：`estimate_eye_gaze` 的符号**保持不取反**。
  几何上也自洽：非镜像相机下使用者向自己的右看 → 虹膜偏影像左（负值），而 `ParamEyeBallX`
  正方向对应观众右，故应发负值 —— 即不取反。**若重启后头正了、眼球仍反**，再单独翻这一处
  （届时只剩一种可能）。
  回归测试：`tests/test_eye_gaze_direction.py`（6 个测试，期望值按"不取反"锁定；
  文件头记录了两次矛盾反馈的完整经过与判定依据）。

- **头部三轴"幅度不够"：源 ±45° 映到 VTS ±30° 的 33% 损失**（2026-09-16）。
  实测（刻意摇到最大）`FaceAngleX` range 55.66、`FaceAngleY` 51.41、`FaceAngleZ` 72.48，
  且 `FaceAngleX/Y` 的**正侧都精确顶在 `+30.00`**（VTS 声明的上限 = 模型最大转头幅度）
  —— 说明感知幅度够、满量程也能到，但**映射是 0.67 倍**：`raw 20° → 只发 13.3°`，
  中等幅度自然显得"摇不动"。按 ±30° 校准后映射变 **1:1**：
  `raw 10 -> 10（原 6.67）`、`raw 20 -> 20（原 13.33）`、`raw 30 -> 30`，**同等转头多驱动 50%**；
  `raw > 30` 被夹在 ±30（与原先"到 45° 才满程"相比只是更早到顶）。改动在
  `apps/vtube_bridge/config/tracking_calibration.json`（新增 `FaceAngleX/Y/Z`）。

- **"眨眼只到眯眼"：感知层眨眼幅度不足，用校准把实测范围拉满**（2026-09-16）。
  修完映射退化 bug 后眼睛能睁开了，但**眨眼仍只到"眯眼"**。高频采样（0.2 s × 45 s）定位：
  ```
  [range] EyeOpenLeft   min=0.251  max=0.988
  [range] ParamEyeLOpen min=0.480  max=1.871     # 0.251 × 1.9 = 0.477 ✓ 映射是忠实的
  ```
  即**尽力闭眼并保持 2 秒，输入仍停在 0.25** ⟹ MediaPipe 的 `eyeBlink` 上限只有 ~0.75
  （感知层幅度不足）。已用实验排除另两种：EMA 平滑（闭 2 秒约 50 帧早该收敛）、
  VTS 侧映射（`0.251 × 1.9 = 0.477` 与实测 `0.480` 吻合）。
  用项目自带的校准机制按实测范围校准（新增
  `apps/vtube_bridge/config/tracking_calibration.json`：`center=0.99`、`min=0.25/0.26`、
  `max=0.99`）→ 实测 `0.251 -> 0.001`、`0.988 -> 0.997`，
  未校准通道（`MouthOpen`、`FaceAngleX`）**保持不变**。
  - **口径**：该范围是在**当前相机 / 距离 / 光线**下测的，换条件需重测。
  - **已知不便**：`--no-gui` 下没有 GUI 校准入口（未装 PyQt5），目前只能手写该 JSON。

- **单向参数被映射成常量：`EyeOpenLeft` / `EyeOpenRight` 恒为 0，皮套整场闭着眼**
  （2026-09-16 当场定位并修复）。`TrackingCalibrationManager._map_value` 用的是"以源中心分成
  两段"的映射：下段映到 `[target_min, target_default]`、上段映到 `[target_default, target_max]`。
  而 `EyeOpenLeft` 的源范围是 `0..1`、源默认是 `1.0`（睁眼），**中心落在端点上** ——
  于是整个源范围都被放进下段；VTS 对该参数报的 `defaultValue` 又恰好等于 `min`（0 = 闭眼），
  **该区间退化成单点**：实测 `0.97 -> 0.000`，感知层给出正常值也照样发 0。
  - **现象**：转头 / 张嘴 / 视线都正常，**只有双眼一直闭着**（模型侧实测
    `ParamEyeLOpen = ParamEyeROpen = 0.0`）。
  - **已排除的层次**（用实验否掉，不是猜）：相机图本身正常（诊断脚本实测
    `eyeBlinkLeft=0.0335 -> EyeOpenLeft=0.967`）、裁剪正常、脸大小无影响、把眼睛裁掉也不影响。
  - **修复**：中心不落在源范围内部时改用全范围线性映射。双向参数（转头 ±45°→±30°、视线）
    走原路径，行为不变。
  - **TDD 证据**：新增 `tests/test_vts_parameter_mapping.py`（7 个测试），RED 实测
    `AssertionError: 0.0 != 0.97 ... an open eye (0.97) must not be mapped to 0 = closed` →
    修复后 **Ran 70 tests / OK**，复现脚本输出由 `0.000` 变为 `0.970`。
  - **补充防护（同日）**：两段式映射的前提是"源中心"与"目标默认值"**都严格落在区间内部**。
    只检查源侧不够 —— 若把 `center` 设在中间（校准功能允许），下段目标区间仍退化成单点
    （`defaultValue == min`），同一个 bug 会**再次**出现（实测 `0.5 -> 0.000`）。
    故两侧任一不可分段即改用线性映射。RED 实测
    `AssertionError: 0.0 != 0.3378 ... a middle center must not flatten the lower segment to the
    target default` → 修复后 **Ran 72 tests / OK**。

- **VTS 授权握手必然失败：`AuthenticationTokenRequest` 用了 1 秒的 socket 超时**
  （`VTubeStudioClient(timeout=1.0)` 对**所有**请求生效），而这个请求的回应是
  **VTS 弹窗等人点"允许"**。VTS 侧日志实测：`[13:05:15] Triggering authentication popup.` →
  `[13:05:16] A plugin has disconnected from VTube Studio API.` →
  `[13:05:21] User granted authentication request ... Returning token.` —— 桥接在 1 秒后放弃，
  点击在 5 秒后到达，**token 被返回到已关闭的 socket，永远写不进 token 缓存**，
  于是每次运行都要重新问一遍。这也解释了为什么"首次运行点允许"这条文档流程从未真正跑通。
  现在该请求单独使用 `AUTH_TOKEN_TIMEOUT = 120 s`（其余请求维持 1 s），
  并在 `finally` 里恢复原超时。按 TDD 落地：先写 4 个会红的测试
  （`tests/test_vts_auth_wait.py`，用假 websocket 记录 `settimeout` 与消息），
  实测 RED `[] is not true` → 修复后 **Ran 63 tests / OK**。

- **订正一条错误建议："拖 `.model3.json` 加载主模型"**。VTS 的拖放白名单里 **`.json` 是禁用类型**
  （日志 `Dragged file of type "WebItemJSON" ... Drag & drop is disabled for this type`；
  启动设置原文 `Blocked file types: ".pdf", ".txt", ".html", ".json"`），而唯一可拖的**文件夹**
  必定走 `Live2D Item` 路径（`Dragged folder is a Live2D Item`）→ **拖放根本加载不出主模型**。
  `apps/vtube_bridge/README.md` 第 2、3 步已改为"VTS 界面模型选择 / API `ModelLoadRequest`"
  两条可行路径。本类失误第 3 次，见 `mistakes.md` 失误 6。

- **订正一处错误结论："8001 未监听"曾被当作实测写入 4 份文档**。上一轮用
  `Get-NetTCPConnection` 得到 0 条记录就下了结论。改用 `netstat -ano` 复核：
  `TCP 0.0.0.0:8001 LISTENING`（PID = VTS）、真实 TCP 连接成功、VTS 日志
  `[VTubeStudioAPI] Started VTube Studio API: ws://0.0.0.0:8001`。已逐处订正
  （`MEMORY.md`、`apps/vtube_bridge/README.md`、`docs/plans/2026-09-15-皮套落地三线.md`），
  并把 `Get-PnpDevice` / `Get-CimInstance` / `Get-NetTCPConnection` 三条**不可信接口**
  写进 `conventions.md` 的「取证红线」。同类错误当日发生两次，见 `mistakes.md` 失误 4 与 5。

- **OpenCV 分发包冲突：三个包共用同一个 `cv2/` 目录，随时可能被 headless 覆盖**。
  实测本机 `opencv-python` / `opencv-python-headless` / `opencv-contrib-python` 三者并存，
  实际生效的是 `opencv-contrib-python 5.0.0.93`（`cv2.__version__ == "5.0.0"`，
  而 `pip show opencv-python` 仍报 4.13.0.92）。headless 构建没有 `imshow` / `waitKey` /
  `destroyAllWindows` / `getWindowProperty` / `WND_PROP_VISIBLE`，而 `src/deploy/detect.py`
  用了它们 —— 一旦 headless 成为"最后安装者"，实时预览会崩，且报错指向 `cv2.imshow`
  而非真正的安装顺序。新增 `tests/test_opencv_environment.py` 作为**护栏**：
  缺符号时测试**失败而非 skip**，失败信息里直接给出修复命令。当前环境实测
  **Ran 59 tests / OK**（改动前 56 / OK）。根因与取舍见 `.dsh-dev/decisions.md`。

- **单图检测会弹出阻塞窗口**：`src/deploy/detect.py` 在单图模式下无条件调用 `cv2.imshow()`
  并进入 `waitKey` 循环，直到按键或手动关闭窗口才返回。这使脚本无法用于批处理与自动化。
  新增 `--no-show` 开关；默认的交互行为不变。

### 变更

- **`requirements.txt` 的 OpenCV 声明改为说真话**：原先声明 `opencv-python>=4.5.0`，
  但 mediapipe 依赖 `opencv-contrib-python`、albumentations 依赖 `opencv-python-headless`，
  pip 会同时装上三者（而它们写的是同一份 `cv2/`）。现改为声明**真正必需**的那一个
  （`opencv-contrib-python>=4.5.0`），并加注释说明"最后安装者生效"与"禁止 headless 收尾"。
  **本次未重装**：重装**收益为零**（不改变"三个包都装着、生效者由安装顺序决定"的结构）而
  **风险非零**（顺序不受控；受限 shell 的 pip 会再生成一份用户目录副本）。
  **本条是处置完成、不是问题根治** —— 脆弱性仍在，根治路径（未做）＝把只用于可视化的第三方增强库
  从运行环境拆出。理由口径订正见 `.dsh-dev/mistakes.md` 失误 9（2026-09-22）。

- **目录重组（阶段 2A）**：从按"人头分工"划分的六目录结构改为标准工程布局。

  | 旧路径 | 新路径 |
  |:---|:---|
  | `data/*.py`、`dataset/` | `src/data/` |
  | `training/`、`model/model_test.py` | `src/train/`（`model_test.py` → `check_model.py`） |
  | `evaluation/` | `src/eval/` |
  | `deployment/*.py` | `src/deploy/`（`realtime_detect.py` → `detect.py`） |
  | `deployment/Vtube-Studio-Bridge/` | `apps/vtube_bridge/` |
  | `model/model_config.yaml` | `configs/model.yaml` |
  | `training/checkpoints/`、`evaluation/reports/` | `artifacts/checkpoints/`、`artifacts/reports/` |

  - 新增 `src/paths.py` 作为**全项目唯一的路径来源**，取代原先散落在 6 个文件中的
    `dirname(dirname(__file__))` + `sys.path.insert` 写法（该写法在脚本进入子包后必然算错项目根）。
  - 所有脚本改为从项目根以模块方式运行：`python -m src.<pkg>.<module>`。
  - Git 全程记录为 `rename`（相似度 72–100%），文件历史完整保留。

- **文档结构分层**：`docs/` 根为现状文档，`docs/archive/` 为历史设计文档（正文不改），
  `docs/plans/` 为实施计划。移除 `docs/superpowers/`。

### 移除

- 删除课程设计相关产物：`_template_full.txt`（课程报告模板）、`_temp_team.txt`（成员分工表）。
  本项目为个人独立完成，不存在人员分工。
- 删除重复文件：`yolo26n.pt`（与 `yolov8n.pt` 字节相同且无任何引用）、交付包目录及其 zip
  （唯一独有资产《使用指南》已先归档到 `docs/`）、12 个训练调试图、全部 `__pycache__`，
  共释放约 49.6 MB。

### 修复（随目录重组一并处理）

| 缺陷 | 原状 | 修正 |
|:---|:---|:---|
| `check_model.py` | 引用 `yolov8n-face.pt`，**该文件从未存在** | 改为 `yolov8n.pt`，与 `configs/model.yaml` 一致 |
| `download.py` | 名称暗示会下载，实际只打印手动下载说明 | docstring 明确写出不执行下载 |
| `check_model.py` | 自称 unit test，却没有任何断言 | 重新标注为诊断脚本 |
| `apps/vtube_bridge/run_gui.bat` | 引用 `..\..\.venv\Scripts\pythonw.exe`，而**项目没有 `.venv`** | 改用 PATH 中的 `pythonw` |
| 桥接 `sys.path` 设置 | 插入 `PROJECT_ROOT`，但 `thirdparty/` 实际位于桥接目录内 —— 仅因当前工作目录恰好正确才侥幸工作 | 改为插入 `BRIDGE_ROOT` |
| 桥接 `DEFAULT_MEDIAPIPE_MODEL` | 写死整条 `deployment/Vtube-Studio-Bridge/...` 路径 | 改为从 `BRIDGE_ROOT` 派生 |

### 文档

- 新增《架构说明》，按功能职责（而非人员分工）描述模块划分、数据流与设计取舍。
- 新增《项目现状与差距》，逐条记录实现与设计承诺的差异，每条附证据。
- 重写 README：所有指标标注测量口径，并如实列出未完成项。
- `.gitignore` 扩展：泛化训练调试图规则，新增运行产物（截图、`results/`、输出视频）
  与重新生成的交付包排除规则；将 `.claude/settings.local.json` 移出版本控制使既有忽略规则生效。

### 首次纳入版本控制

- `apps/vtube_bridge/`（VTube Studio 表情驱动）—— 此前从未提交，项目最核心的应用部分。

### 已知问题

- WIDER Face Easy/Medium/Hard **分项评估未完成**，原始 0.76 目标无法对账。
- ONNX 精度验证因 `data/annotations/val_list.txt` 缺失而**静默跳过**，"MAE < 1e-4" 无实测支撑。
- **ONNX 前向比 PyTorch 慢约 3 倍**（22.3 ms vs 6.9 ms），且 benchmark 不打印实际生效的
  ExecutionProvider，运行设备待确认。
- Recall 仅 0.595，小脸与遮挡场景漏检明显。
- v2 微调为**负收益**（mAP50 0.66349 → 0.66171），缺困难样本回流。
- `src/data/augment.py`（Albumentations）未接入训练流程。
- WIDER 标注解析存在 **4 份重复实现**，边界处理不一致。
- 设计中的自采 500 张数据未采集。
- 6 GB 显存下训练超额分配（峰值 11.1 GB），吞吐显著下降。

---

## [2026-09-15] 测量与修复

### 新增（项目此前从未有过的实测数据）

- **ONNX 导出精度按检测质量裁定**，而非张量误差。同一组权重在全部 3,226 张验证图上对比：

  | 变体 | mAP50 | mAP50-95 | P | R | 吞吐 | 设备 | 体积 |
  |---|---:|---:|---:|---:|---:|:---|---:|
  | PyTorch `.pt` | 0.66404 | 0.35618 | 0.8479 | 0.5937 | 87.8–147 FPS | CUDA | 5.95 MiB |
  | ONNX **FP16** | 0.66382 | 0.35627 | 0.8448 | 0.5959 | ~36 FPS | CPU | **5.88 MiB** |
  | ONNX FP32 | 0.66379 | 0.35670 | 0.8446 | 0.5959 | ~36 FPS | CPU | 11.70 MiB |

  结论：**FP16 的检测质量与 PyTorch 无差别**（mAP50 −0.0002 / mAP50-95 +0.0001），体积减半。
  导出改为 `half=True`，项目 ≤10 MB 目标重新满足（FP32 为 11.70 MiB，一度超标）。

- **按人脸尺寸分层的检测分析**（39,708 张标注人脸）：汇总召回 0.610，但 **72.5% 的 GT 人脸
  <32px、召回仅 0.493**；**≥32px 人脸召回 0.916**。这修正了"召回偏低是模型短板"的判断 ——
  主因是 WIDER 基准中压倒性的极小脸，而非实际场景不可用。
- **运行环境实测**：`onnxruntime` 1.27 仅提供 `CPUExecutionProvider`（无 CUDA，且因无网络
  无法安装 `onnxruntime-gpu`），因此 ONNX 比 PyTorch/CUDA 慢 **2.4–3.3 倍**。
- 为 v1/v2 保存的 best 权重重新测得 mAP，取代此前引用的训练日志末行（最后 epoch）数字。

### 性能

- **VTube Studio 桥接默认模型由 `.onnx` 改为 `.pt`**。`onnxruntime` 在本机只暴露
  `CPUExecutionProvider`，所以桥接此前一直在**CPU 上做 YOLO 推理（中位 24.8 ms）而 GPU 闲置**。
  改为 `.pt` 后走 CUDA 并自动启用 FP16。无头验证：`device=0`、`format=PyTorch`、`use_half=True`、检出 8 张人脸。
- **实测确认 FP16 默认值正确**（交替 A/B，5 轮 × 15 样本，同进程）：
  `half=True` 中位 **12.52 ms / 79.9 FPS**，`half=False` 中位 **13.52 ms / 73.9 FPS**，FP16 快 **7.4%**。

### 测量方法

- **发现本机测量噪声约为 ±50%**：同一调用跨度达 **4.8 – 18.8 ms**（约 3 倍）。
  本会话中所有"同一数字对不上"的现象都源于此，而非代码或环境变化。
- `src/deploy/benchmark.py` 因此不再只报一个平均值，改为输出
  **中位数 + 最小值 / p95 / 最大值 + 样本数**，并在开头说明原因。
  最新中位结果：PyTorch **10.31 ms / 97.0 FPS**，ONNX **24.80 ms / 40.3 FPS（CPU）**；
  体积检查随 FP16 生效而重新 **PASS**（`≤10MB`），此前 FP32 为 11.70 MiB 属 **FAIL**。
- 结论：**任何单次读数不可作为结论**，引用速度必须注明测量次数与统计量。

### 修复

- `split.py` 解析 WIDER 标注时崩溃（`num_faces=0` 的假框行未跳过），导致 `train_list.txt` /
  `val_list.txt` **从未成功生成**，进而使 ONNX 精度验证与分项评估双双静默失效。
- `export_onnx.py` 把 ONNX 拷贝到自身路径，抛 `SameFileError` —— 导出脚本无法运行。
- 清单文件按平台默认编码（GBK）写入，中文绝对路径被破坏；写入与读取统一为 UTF-8。
- `evaluate.py` 的 P-R 曲线查找 `PR_curve.png`（ultralytics 实际写 `BoxPR_curve.png`）且无 else
  分支，**静默不产出任何文件**；同时消除每个模型的第二次冗余 val（4 轮 → 2 轮）。
- `evaluate.py` 在官方评测包缺失时把 mAP50 填进 Easy/Medium/Hard 三个字段**冒充真实结果**；
  现返回 `null` 并显式说明不可用。
- `analyze_errors.py` 的误差分布图建立在 **20 个样本**上（且是"最小的 20 个漏检"）；重建为
  基于全部错误的统计，输出**数字表格**，并改为分析实际部署的 v2 权重。
- `validate_precision()` 在 `val_list.txt` 缺失时 `return True`（判定通过），把跳过当通过；
  现明确报告 `[SKIPPED]` 且不视为通过。
- 精度判据由固定 `1e-4` 改为按精度区分（FP16 `1e-1` / FP32 `1e-4`），并说明张量 MAE 的含义边界。
- 单图检测弹出阻塞窗口（`cv2.imshow` + `waitKey` 循环），无法用于批处理 → 新增 `--no-show`。
- WIDER 标注解析的 **4 份重复实现**（`convert` / `qc` / `split` / `loader`）合并为
  `src/data/wider_annotations.py` 单一来源；`qc.py` 另有一处 `cv2.imread(...).shape` 缺 None 检查。

### 文档

- README：新增按尺寸分层表与运行方式建议；修正 v2 结论（持平，而非负收益）、ONNX 为 FP16，
  补充单位（MB/MiB）与测量次数的说明。
- 使用指南、面试讲解提纲：同步上述全部修正，含 ONNX 精度判据的完整解释。
- 《项目现状与差距》新增第七节：状态更新、9 个新差距、可复现命令。

### 新增：置信度阈值扫描（G3）

`src/eval/conf_sweep.py` —— **不重跑推理**：检测 AP 与阈值无关，`model.val()` 本来就计算完整
P-R 曲线并沿置信度轴暴露 P/R/F1，因此一次验证即可读出所有工作点。

| conf | precision | recall | F1 |
|---:|---:|---:|---:|
| 0.10 | 0.5812 | 0.6746 | 0.6244 |
| 0.20 | 0.7560 | 0.6308 | 0.6877 |
| **0.25（默认）** | 0.8139 | 0.6097 | **0.6971** |
| 0.30 | 0.8581 | 0.5887 | 0.6983 |
| 0.50 | 0.9537 | 0.4909 | 0.6482 |

max-F1 点为 `conf=0.297`、F1 0.6985。

**结论：默认 `conf=0.25` 几乎正是最优 F1 点**（相差 0.0014）。此前文档中"`conf=0.25` 对召回
不友好"的说法**被实测推翻**。AP50 与阈值无关，阈值只用于选取工作点；本项目未改动任何默认阈值。

### 新增：单元测试（G18）

`tests/` 共 **56 个测试**，使用标准库 `unittest`（本机无网络，装不了 pytest），覆盖
WIDER 标注解析、WIDER→YOLO 坐标转换、尺寸分层结论所依赖的 IoU / 分桶几何，以及**桥接的时序
滤波**（`FaceBoxFilter` 的 EMA/中值/hold/reset、`ParameterFilter`、人脸框到 VTS 位置的映射）。
fixture 采用**仓库内提交的文件**而非运行时临时文件——受限沙箱会拒绝写入新建目录，
而**被跳过的测试不是安全网**。

针对桥接滤波的**判别性用例**：中值抑制（`alpha=1` 隔离中值滤波，稳定窗口内 6 像素的偏移必须被
丢弃）、以及远距离跳变重置（`alpha=0.05` 时应**精确跳到**新脸而不是缓慢靠近）。

### 修复（由新测试推导出来）

- **`parse_samples()` 存在生成器/`with` bug**：它在 `with open(...)` 内 `return parse_lines(f)`，
  而 `parse_lines` 是生成器，函数体在迭代时才执行，此时文件已关闭 →
  `ValueError: I/O operation on closed file`。**该 bug 会使 `src/data/split.py` 失效。**
  已改为先读完全部行再委托；并新增"同一 fixture 读取两次"的回归测试。
- **空行容忍只做了一半**：docstring 声称空行可容忍，实际只在图片名之前跳过，
  **图片名与数量之间、数量与框之间都不跳**。两处现已一致。

### 性能（G17）：训练显存实测，`batch` 由 8 下调为 6

新增 `src/train/probe_vram.py`（1 epoch / 5% 数据切片 / workers=0），实测峰值显存：

| batch | 峰值**分配** | 峰值**保留** | 占 6.00 GiB 物理 |
|---:|---:|---:|---:|
| 4 | 1.20 GiB | 3.58 GiB | 60% |
| 6 | 1.75 GiB | **4.41 GiB** | 73% |
| 8 | 2.29 GiB | **6.18 GiB** | **超过上限** |

关键：**分配量只有保留量的约 1/2.5**，与显存上限比较的应是**保留量**。`batch=8` 单轮即
6.18 GiB，已超过 6.00 GiB 物理上限，从第一轮起就向共享内存溢出；100 epoch 长跑因内存碎片
继续增高（历史日志达 11.1 GiB），吞吐从 5.8 it/s 崩到 1.7 it/s。

**`configs/model.yaml` 的 `batch_size` 已由 8 改为 6**（4.41 GiB，留 27% 余量）；若需更大余量可再降
到 4。**尚未用新 batch 重训验证实际收益。**

> 本次探针的**耗时不具可比性**：`batch=4` 那轮包含 `yolo26n.pt` 下载与首次初始化，且 161 步的
> 短跑无法体现长跑碎片化。只有显存数据有效。

### 环境：出网由沙箱控制，且不稳定

受限模式下 `github.com` 与 `pypi.org` **均不可达**；提权后可达但**不稳定** —— 同一会话中 pip 能
拉取仓库，`git clone` 却以 `Connection was reset` 失败；官方评测包的 `setup.py` 也无法在当前
setuptools 下构建。

因此 **G9/G2 的官方分项指标（Easy/Medium/Hard）无法在本环境可靠取得**，维持尺寸分层的替代方案
（它直接指出失效模式，比按基准自带难度分桶更有信息量）。

顺带确认：`yolo26n.pt` 由 **ultralytics 的 AMP 检查自动从 GitHub 下载**（本次为 5.29 MB），
不是项目产物 —— 此前删除的那个（6.2 MB，与 `yolov8n.pt` 字节相同）确属误命名副本。

### 新增：`src/deploy/compare_export_variants.py`

把"三种导出变体的 mAP 与吞吐对比"固化为可复用脚本（原先是一次性探针），
其结论已写入 README 与《决策记录》。

### 变更：Albumentations 管线的定位（G12，采用方案 B）

`src/data/augment.py` 从未进入训练，只被 `vis_aug.py` 引用。经评估**不接入训练**，定位为
**增强可视化工具**并写入文件头说明。理由：

- 其全部变换已被 ultralytics 内置增强覆盖（`fliplr` / `hsv_*` / `randaugment` 含模糊 /
  `erasing` 即 Cutout / letterbox）；
- 且**缺 Mosaic**（ultralytics 在检测上最有价值的增强）→ **替换会严格变差，叠加会让翻转与
  HSV 双重施加**。

顺带纠正：设计文档承诺的"后 50 epoch 加 Cutout/模糊"**其实早已被 ultralytics 默认值满足**。

**若将来要做数据侧提召回，重点是两条**（已写入 `docs/项目现状与差距.md` 第八节）：

1. **属性感知增强** —— 用 WIDER 标注自带的脸级 `blur` / `illumination` / `occlusion` / `pose`
   属性做定向增强；ultralytics 看不到这些字段（对应 G14）。依据：≥32px 人脸召回已达 0.916，
   剩余失误集中在困难子集。
2. **小脸放大裁剪** —— 随机裁出含小人脸的区域放大到 640。依据：72.5% 的 GT 人脸 <32px、召回 0.493。

真实成本：关闭重叠变换、自行实现 Mosaic、打通 ultralytics 的 Dataset / Transform 层。
**第一步应是补训练数据路径的测试**，而非直接改 `augment.py`。

文档同步：README、`docs/架构说明.md`、`docs/项目现状与差距.md`（新增第八节）、
`.dsh-dev/decisions.md`。

### 修复：`CoarseDropout` 参数被静默忽略

做出 G12 决策后校验时发现，`src/data/augment.py` 的 `CoarseDropout` 用的是 **Albumentations 1.x
的参数名**（`max_holes` / `max_height` / `max_width`），而项目安装的是 **2.0.8**。这些参数
**只产生一条 `UserWarning` 后被忽略**，实际生效的是默认值：

| | 预期（1.x 写法） | 实际生效（2.x 默认） |
|:---|:---|:---|
| 洞的数量 | 最多 8 个 | `num_holes_range=(1, 2)` |
| 洞的尺寸 | ≤32 px | `hole_height_range=(0.1, 0.2)`（即 10~20%） |

已改用 2.x API（`num_holes_range=(1, 8)` / `hole_height_range=(1, 32)` /
`hole_width_range=(1, 32)`），并用 `-W error::UserWarning` 验证构造时不再抛警告。
`requirements.txt` 相应收紧为 `albumentations>=2.0.0`。

同时把 `widerface-evaluate` 标注为**可选依赖**并说明：项目实际汇报的指标不依赖它
（缺失时 Easy/Medium/Hard 报 `null`，分项分析由按尺寸分层替代）。

### 文档订正：定向增强的载体（`docs/项目现状与差距.md` 新增 8.5）

查证 ultralytics 8.4.75 源码后，订正了此前两处判断：

1. **载体不是"Albumentations vs 自定义 Dataset"**。`Albumentations` 是库；真实载体是
   **`augmentations` 超参钩子**（`v8_transforms()` 已内插 `Albumentations(transforms=hyp.augmentations)`，
   一行配置即可，且**会自动同步标注框**）与**自定义 Dataset**（数天级）。
2. **"必须自行实现 Mosaic"是错的**：钩子运行在 Mosaic / affine / MixUp **之后**，Mosaic 原样保留。

并记录了钩子的一个隐蔽陷阱：`contains_spatial` 靠**硬编码类名白名单**判断，**自定义变换类名不在表内
→ 被当作纯像素变换 → 框静默不同步、标注被破坏**。

据此给出两条定向增强与载体的对应：**属性感知增强必须走自定义 Dataset**（标注属性进不了钩子的
`image + bboxes + class_labels` 通道，且需先补 G14 的属性解析）；**小脸放大裁剪可走配置级钩子**
（用白名单内的 `BBoxSafeRandomCrop` + `LongestMaxSize`，无需自定义类）。

推进顺序因此改为：**先用配置级钩子零代码验证小脸放大裁剪是否真能提升召回，有效再投入 Dataset 级改造。**

### 实验：小脸裁剪放大 —— 代理方案不成立（G12 / 差距清单 8.6）

新增 `src/train/probe_aug_crop.py`（A/B 框架：两 arm 均从 `best_model_v2.pt` 微调 3 epoch，
唯一差别是 `augmentations`）。实测结果：

| 尺寸桶 | base | crop | Δ |
|:---|---:|---:|---:|
| **Tiny（<32px）** | 0.494 | **0.501** | +0.007 |
| Small（32–96px） | 0.902 | 0.897 | −0.005 |
| Medium（96–256px） | 0.959 | 0.952 | −0.007 |
| Large（>256px） | 0.991 | 0.986 | −0.005 |
| 总体召回 | 0.610 | 0.614 | +0.004 |
| **精确率** | **0.808** | **0.765** | **−0.043** |
| 预测总数 | 29,993 | 31,874 | +1,881 |

**多命中 147 张人脸的代价是多输出 1,881 个预测（约 13:1）** → 代理方案无效：收益不显著而代价明确，
**不足以支撑 Dataset 级改造**。机制推测：裁剪放大抬高了模型对大脸的置信度分布，推理时过度自信。

**这不能完全否定"定向"版本**（本实验用的是随机裁剪，不做"含小脸"的定向选择），但结论明确。
局限：仅微调 3 epoch、单次运行无种子重复、`Resize` 会拉伸长宽比、测的是随机而非定向裁剪。

**下一步的高杠杆方向**：既然根因是"<32px 的脸在 640 输入下不足 10px"，直接提高分辨率
（`imgsz=960`）作用于同一根因，且同一个 A/B 脚本只改 `--imgsz` 即可验证。

### 过程中修掉的三个问题

- `probe_aug_crop.py` 传入整个 config 却直接索引 `cfg["optimizer"]`（该键在 `cfg["training"]` 下）→ `KeyError`；
- **`fraction < 1` 会强制重建标签缓存**（`base.py` 在 `get_labels()` 之前截断 `im_files`，哈希失配）；
  更关键的是 `fraction` 取的是**排序列表前缀而非随机抽样** —— WIDER 按事件类目录排序，
  `fraction=0.25` 等于只保留前 25% 的事件类别，**不是代表性样本**，据此比较召回会得出错误结论。
  已改为 `fraction=1.0`；
- 磁盘上的标签缓存曾被 `fraction=0.05` 的探针写成**子集缓存**，导致后续全量训练全部哈希失配 →
  一次性提权重建后恢复正常。

### 文档：新增《简历项目经历》撰写稿（游戏客户端 / 客户端开发方向）

新增 `docs/简历项目经历.md`。定位是**工作文档，不是简历本身**。首轮稿偏"技术专业化"，
经反馈后按**「先过筛选，再谈专业」**重写：简历的读者顺序是 HR/关键词初筛 → 技术面，
一份稿子同时讨好两边的结果是两边都过不了。

- **区分两个筛子**：① 关键词初筛的死因是**岗位关键词零重合**（JD 要 Unity/C#/C++，稿子全是
  WIDER/mAP/ONNX/letterbox）；② 技术面第一眼觉得"投错岗了"。对策相反 —— 客户端版**一个 mAP
  都不出现**，算法口径降为备用版。
- **新增 JD 关键词对照表**：列出 7 项可对应的真实经历（实时帧预算、多线程、性能定位、工具开发、
  SDK 协议对接、手感调优、工程规范）与 2 项**硬缺口（C#/Unity、C++）**，并指出补 Unity +
  Live2D Cubism SDK 是这份项目性价比最高的改造。
- **新增"术语翻译对照表"**：同一件事的算法说法 ↔ 客户端说法（如"三级时序滤波"→"解决抖动、
  闪烁、切换滑动三类手感问题"）。
- **A/B 两版桥接措辞**：皮套**尚未真机验证**，故给出现可用（"已落地 + 联调进行中"）与跑通后
  （"已完成并验证"）两版句子及替换对照表，避免提前宣称已验证。
- **禁止写清单**：逐条列出会当场翻车的表述（mAP 0.76、162.8 FPS、ONNX 加速、自采 500 张、
  六人分工、"~6 ms"检测延迟等）及其实测反证。
- **顺手发现的两处不一致**：① README/面试提纲/架构说明的"**~6 ms** 检测单帧"与
  `docs/使用指南.md` 实测**中位 12.5 ms** 冲突，简历应改用 benchmark 有日志支撑的 10.3 ms
  （此项**未改**，需实测后统一）；② `MEMORY.md` 的精度 **0.663 / 0.662 为旧值**，与 README 及
  `artifacts/reports/metrics_*.json` 的 0.66501 / 0.66404 不一致 —— **已修正**，同时订正同表
  "训练超参 batch=8"（配置已改 6）与"主要短板"三项已过时表述（ONNX 精度验证已补、"v2 为负收益"
  应为止平）。

### 文档订正：简历稿的角色定位于"决策者"（去除"自研"类表述）

经用户澄清：本项目是 **AI 辅助产出的产品，用户担任决策者**，此前简历稿按"实现者"口径写
（"自研滤波方案""自研调试面板""独立完成从 0 到 1""我写了 56 个测试"）属**定位错误，非措辞问题**。
`docs/简历项目经历.md` 据此重写：

- 新增「角色重定义」表：逐条给出"实现者口径 → 决策者口径"的替换
  （自研 → 确定/选定/否决/验收；独立完成 → 主导）；并把"**决定不自建检测模型**"
  改写为产品判断（成熟任务不重复投入，预算集中在管线工程）。
- 新增「决策者必答：你的决策清单」：从 `.dsh-dev/decisions.md` 抽取 **10 条带证据的真实决策**
  （判据由张量 MAE 改为 mAP、"未测量不算通过"、否决通用增强接入训练、batch 8→6、
  桥接默认 .onnx→.pt、推翻此前 2 条结论等），每条附"候选方案 + 依据 + 结果"的出处，
  作为"决策者"身份的唯一硬支撑。
- 新增「AI 辅助要不要写进简历」：给出不显式（默认，投游戏客户端）与显式（投 AI 产品方向）
  两条路及各自风险，并要求**面试时主动交底**。
- 禁止写清单首条改为 **"自研 / 独立完成 / 从 0 到 1 实现 / 我写了 X"**，理由是角色定位而非措辞。

### 文档：简历稿新增「正式版」与旧稿审计（5 处与仓库文档矛盾）

用户提供的早前 Claude 稿（项目描述 + 技术栈 + 技术特点）经逐条核对，**16 条中 5 处 ❌ 与仓库
文档直接矛盾**，会与 README「已知限制」、CHANGELOG「已知问题」、差距表自相打架。据此：

- 新增 **3.4 正式版**：沿用旧稿三段式结构与 Vibe Coding 定位，事实校正为 —— ONNX 定位为
  可移植性而非部署主路径；增强交由 ultralytics 内置（未接第三方管线）；**batch 8 → 6 的显存
  实测依据**取代"12GB 小卡 + 梯度累积"；按尺寸分档统计的是**召回**而非 mAP，官方 Easy/Medium/
  Hard 分项未取得一律报 `null`；478 关键点；分步可复现流程取代"一条命令跑通"。
- 新增 **第十一节 旧稿审计**：16 条逐条判定与证据，重点标注 5 处 ❌ ——
  ① "依托 ONNX Runtime 完成推理部署"（本机无 CUDA provider，桥接已改回 `.pt`）；
  ② "梯度累积在 12GB 小卡上模拟大 batch"（6 GB；`accumulation_steps` 是死键）；
  ③ "按尺寸分 Easy/Medium/Hard 三级评测 mAP"（难度子集 ≠ 尺寸分档，且未算 mAP、官方分项为 null）；
  ④ "468 个 3D 关键点"（应为 478）；⑤ "一条命令跑通全流程"（下载需手动，无 pipeline 脚本）。
  另标注 4 处 ⚠️：多模态术语误用、Albumentations 未接入训练、越界框剔除无实现、训练无模糊增强、
  "静默降级"措辞反了。
- 记录一条判断：**这类矛盾损失的不是单条加分，而是整份简历的可信度**，而"每个数字都标口径、
  未完成如实列出"恰是本项目唯一的差异化优势。

### 新增：皮套落地计划（Unity + Live2D / VRM）与授权红线

用户明确最终产物需在**虚拟皮套**上跑通，形态为 **Unity + Live2D Cubism SDK / UniVRM 自集成**，
且**当前尚无皮套模型**。据此新增 `docs/plans/2026-09-15-皮套Unity落地.md`，更新
`docs/简历项目经历.md` 第五、六节与 JD 关键词表，并把形态决策记入 `.dsh-dev/decisions.md`：

- **架构决策**：Python 管感知（复用已验证的检测/关键点/滤波），Unity 管表现（参数接收 + 映射层），
  两侧以 UDP/WebSocket + 固定参数协议解耦；纯 C# 一体化（Unity 内跑已导出的 ONNX）列为后续加分项而非起点。
- **验收标准 7 条**：张嘴/眨眼/转头/视线跟随 + 连续 ≥5 分钟稳定 + 可报告的帧率延迟 + 演示视频；
  其中"连续 5 分钟"与"演示视频"最易跳过，但只有它们能支撑简历上的"稳定运行"。
- **★ 授权红线（新发现）**：Live2D 官方规定"**可加载任意数量模型**"的通用追踪软件属于
  **Expandable Application**（[定义](https://www.live2d.com/en/sdk/license/expandable/)），
  发布前须审批 + 签专门协议，**个人与小规模企业不再豁免**，且"通常完全免费不予批准"。
  故本项目**皮套必须绑定固定模型，不得做成可加载任意模型的通用工具** ——
  对公开到 GitHub 的作品集项目尤其关键。开发/试用阶段本身免签约，个人发布非 Expandable 内容豁免。
- **模型来源优先级**：VRoid Studio 自建 VRM（免费、归你、授权最干净）→ Live2D 官方示例模型
  （须标注版权声明；Hiyori 等角色不得改动设计）→ 购买/委托。
  （VRoid 商用条款页与 UniVRM README 本次抓取被反爬拦截/失败，未能逐字核对，已在文档中标注存疑。）
- **简历措辞由两档扩为四档**：A（VTS 代码完成未联调）/ **A′（Unity 侧参数通、皮套未接，即 P0 占位物体
  阶段）** / **B′（Unity + 皮套端到端跑通，推荐目标）** / B（VTS 路线达成），并明确**不得越档写**。
  JD 关键词表中 **C# / Unity 由 ❌ 硬缺口改为 🟡 补中**（跑通后转 ✅）。

### 新增：皮套接线的 P0 实现（UDP 参数出口 + Unity 接收端）

按落地方案实现"不需要皮套就能验证协议"的最小闭环，桥接的捕捉管线**未复制**，只新增出口：

- **桥接新增 `--sink vts|unity|both`、`--unity-host`、`--unity-port`（默认 39540）**，
  新增 `UnityUdpSink`：与 `VTubeStudioWorker` 同构 —— 主循环只发布"最新快照"，
  daemon 线程持有 socket 并按 `--send-fps` 限频发送，**UDP 无连接语义，Unity 未启动/重启/崩溃
  都不会拖慢捕捉循环**。Unity 出口的取值语义见下方"修复"条：按源范围裁剪、不做目标映射，
  由 Unity 侧自行映射到模型参数（这正是"两侧解耦"的落地形式）。
- **发送端契约**：每包必带完整 16 参数，字段名与 C# 侧逐字对应
  `{"seq", "t", "face_found", "parameter_values":[{"id","value","weight"}]}`；包内带 `seq` 供接收端丢弃乱序/重复包。
- **修复一处契约缺陷（由自检发现）**：发送线程原本会在第一次 `update()` 之前先发一个
  `parameter_values: []` 的占位空包，迫使接收端特判一个永远用不上的包型 → 改为 `seq == 0` 时不发送。
- **新增 `apps/unity_link/`**：`mock_receiver.py`（**假 Unity**，仅标准库，装 Unity 前就能验证发送端）、
  `selfcheck.py`（自检，直接实例化真实 `UnityUdpSink` 而非复制实现）、
  `UnityProject/Assets/Scripts/` 下 3 个 C# 脚本（接收器含后台线程与 `seq` 去乱序、映射器含
  VRM/Live2D 接入注释与轴向符号开关、HUD 诊断面板含静默检测），以及含命令级步骤与排障表的 `README.md`。

**已验证（本机实跑）**：`selfcheck.py` → `[PASS]`，40 帧发、38 包收（UDP 丢包属正常，正是 `seq` 的用途），
16 个参数齐全且均为 float、序号单调、`face_found` 正确；`python apps/vtube_bridge/main.py --help`
显示三个新参数；`tests/` 56 个用例仍全部通过。
**未验证**：C# 脚本**未经 Unity 编译**（本机无 Unity），占位物体跟随与坐标轴向符号需实机确认。

### 修复：桥接的 GUI 硬依赖卡住 P0 —— 新增 `--no-gui`

**失误 1（用户发现，已记入 `.dsh-dev/mistakes.md`）**：上一条给出的 P0 第一步命令
`python apps/vtube_bridge/main.py --input 0 --sink unity` **实际跑不起来** ——
`run_bridge()` 里 `if QtWidgets is None: raise RuntimeError("PyQt5 is not available.")` 是硬依赖，
而本机 **PyQt5 未安装**（`requirements.txt` 第 8 行声明了 `PyQt5>=5.15.0` 但环境里没有），
缺了它连摄像头都不会打开。根因是交付"可执行步骤"时只验证了 `--help` 与旁路自检，**没有跑主程序本身**。

- 新增 `--no-gui`：用 `HeadlessDebugWindow` / `_HeadlessApp` 顶替 Qt 窗口，
  **不装 PyQt5 也能跑 `--sink unity` 或 `--sink vts`**，用 Ctrl+C 停止；
  无窗口模式跳过整帧叠加绘制（没有窗口可显示），因此 headless 下 `debug` 计时约为 0（真实值）。
- 缺 PyQt5 且未加 `--no-gui` 时，报错信息改为直接指向该开关（行为不变，仅提示更明确）。
- 已静态核对 `run_bridge` 用到的 8 个窗口接口（`closed` / `target_center` / `camera_preview_enabled` /
  `show` / `close` / `update_frame` / `update_tracking_snapshot` / `processEvents`）全部由替身覆盖，
  并实例化验证通过。**仍未用真实摄像头实跑**（本机不具备条件）。

### 文档订正：简历稿移除未运行过的 PyQt5 调试面板表述

**失误 2（同一根因，已记入 `.dsh-dev/mistakes.md`）**：简历稿把"PyQt5 16 参数实时监视面板、
点击选脸、一键回中"写成已完成能力并列入技术栈，但 PyQt5 未安装 → **该面板在本机从未运行过**。
撰写时沿用了 README / 面试提纲的既有表述，把"代码里有"当成了"已经跑过"。

- 3.1 / 3.2 / 3.4 三处**移除 `PyQt5`**，并加注说明为何移除、何时能加回来。
- 3.3 对照表该行改为"**本机未运行，暂不写**"，改用 Unity 侧调试 HUD。
- JD 关键词表"工具 / 调试可视化"由 ✅ 降为 🟡（待 P0 跑过后确认）；
  「角色重定义」表"提出并**验收**"改为"**提出需求**"。
- 新增 `.dsh-dev/mistakes.md`（失误记录），并在 `conventions.md` 增加一条硬性约定：
  **对外表述只能按"设计 / 源码 / 运行 / 有日志截图"四个层次中实际达到的那一层来写**。

### 修复：`both` 模式下 Unity 出口的双重映射

`both` 模式下两个出口原本共用一份 `mapped_values`，而那份值是**按 VTS 返回的参数范围映射过的**；
Unity 拿到后还会再映射一次到模型参数 → **双重映射**，数值区间被压扁，
因此 `both` 并不能当作"同一组数字发两家"的干净对照。

- 改为**按出口分别计算**，但都来自同一份 `tracking_values`：
  **VTS → 按 VTS 参数范围映射后的值**（校准在此生效）；
  **Unity → 只按源范围裁剪、不做目标映射**（`input_parameters` 传空即此语义），由 Unity 侧自行映射。
  两个出口仍共享同一份感知结果，因此对照实验的前提成立。
- **已验证**（纯函数级实跑）：构造 `FaceAngleX=45`（在源范围内、超出假想的 VTS 范围 −30..30）后，
  VTS 出口得 `+30.00`（按其范围裁剪），Unity 出口得 `+45.00`（原始值），两者不再相同；
  `tests/` 56 个用例与 `selfcheck.py` 均仍通过。

### 记录：原始设计文档中不存在 VTube Studio（VTS 选型的由来无据可查）

核查 `docs/archive/2026-06-22-设计规格书.md` 与 `2026-06-22-实施计划.md`（共约 83 KB）：
**全文没有出现 VTube Studio / Vtuber / 虚拟形象 / 面部捕捉 / 表情驱动**，
两文档中"模块 F 部署与集成"的交付物只有三个脚本：`export_onnx.py`、`benchmark.py`、`realtime_detect.py`。

即：**VTS 不在原始设计与实施计划之内**，是后期以 `deployment/Vtube-Studio-Bridge/` 形式加入的，
且据本文件早前条目，该目录长期**从未纳入版本控制**（"首次纳入版本控制"时才提交）。
**因此"当初为什么选 VTS"在当前项目记录中没有依据可查** —— 这是一个缺失的选型记录，
同理也没有任何一条记录说明它为什么能满足当时的目标。

> 对外的措辞建议：不要把 VTS 说成"设计时就选定的方案"；
> 它是为"让检测结果能驱动一个现成虚拟形象"而引入的**后期集成**，
> 其下架原因（出口不可控、补不了 C#/Unity 技能、通用追踪软件的授权约束）见
> [`docs/plans/2026-09-15-皮套落地三线.md`](docs/plans/2026-09-15-皮套落地三线.md)
> （当时记为 `2026-09-15-皮套Unity落地.md`，该文件后续已被三线方案取代并删除）。

### 核查：VTS 链路从未运行过（连带订正简历措辞档位）

承接上一条，进一步核查"VTS 到底有没有跑通过"，三项证据一致指向**从未运行**：

| 检查 | 结果 | 含义 |
|:---|:---|:---|
| `%APPDATA%\FaceDetectionVTubeStudioBridge\vts_token.json` | **不存在，且该目录本身不存在** | 桥接的 token 只在首次授权成功后写入 → **从未与 VTS 建立过连接** |
| `apps/vtube_bridge/config/tracking_calibration.json` | **不存在（config 目录都没有）** | 校准面板从未保存过任何值 |
| 本机是否安装 VTube Studio | **常见安装位置下未找到** | VTS 本身就没装 |
| （另）PyQt5 | 未安装 | `main.py` 会在 `raise RuntimeError` 处直接退出 |

**结论**：VTS 那条路是**"写完了的集成实现"，不是"跑通的 MVP"** —— 它没有通过任何一次端到端运行，
连"能演示"这一步都没到。项目里**真正有实测支撑的是感知层与模型层**（训练/mAP/吞吐/ONNX 精度/桥接的单帧推理与滤波测试），
**从未验证过的是表现层**（摄像头 → 参数 → 出口 → 皮套这一整条）。

- 简历稿第五节措辞档位据此增补 **A⁻ 档**（当前真实状态：未联调），原 A 档改为"VTS 环境与模型齐备、
  至少真机跑通一次"才可用；原稿那档写的"皮套真机联调**进行中**"属未验证表述，已订正。
  A⁻ 与 A 的区别印证了 `.dsh-dev/mistakes.md` 两条失误的同一根因：**"代码写完"不等于"跑过"**。
- 这正是 Unity 路线 P0 的设计理由：**先用"占位物体 + `--no-gui` + 不需要模型/SDK/VTS"的最小路径，
  把"能跑通"变成可执行、可观察、可留证据的事实**，再往上加皮套。
  VTS 那条路的顺序是反的 —— 先把整套集成写完（参数、滤波、校准、重连），最后才发现跑不起来。

### 决策：皮套出口只走 Unity，VTS + PyQt5 不再投入

用户确认当前产出水平为"**摄像头/视频检测能正常出结果**"，下一步目标是落到**皮套**上，
并明确**选择 Unity 方案、VTS + PyQt5 不做**（记入 `.dsh-dev/decisions.md`）。据此：

- `apps/vtube_bridge/` 的 VTS 出口代码**保留为历史存档**，不再联调，也不再作为简历措辞档位；
  原"可交付备选"的定位作废。**不安装 PyQt5**，桥接一律以 `--no-gui` 运行。
- `--sink vts|both` 两个开关保留 —— 它们现在的意义是证明"同一份感知结果可喂给不同出口"
  这条架构决策，而不是 VTS 本身的价值。
- 简历稿第五节由 **5 档收敛为 2 档（A′ / B′）**，VTS 相关档位全作废；
  降级目标从"回到 VTS 备选"改为"**停在 A′ 档**"。
- **现状口径提醒（供演示素材准备）**：仓库里能证明检测跑通的证据是
  `artifacts/reports/test_detect_0..5.jpg`（6 张标注图）与 `output_result.mp4`
  （720×1280 / 240 帧 / 30 FPS / 约 8 秒）；两者都是**文件输入**的产物，
  **没有留下"实时摄像头（`--input 0`）"的证据**。若要用它支撑"实时"这个说法，
  建议补一张摄像头实时窗口截图或一段录屏（`detect.py` 运行中按 `s` 可截图）。

### 新增：VMC 协议出口（速通线）与三线方案合并

**决策背景**：核查后发现一条此前遗漏的现成标准 —— **VMC Protocol**（VirtualMotionCapture Protocol，
MIT，OSC over UDP）。它的角色定义恰好套上本项目：我们的工具就是标准的 **Assistant**
（只发部分骨骼 + 面部表情给 Performer），规范约定 Assistant → Performer 走 **39540**。
也就是说**不写任何引擎代码**，按规范发 blendshape 就能驱动 VSeeFace 里现成的 VRM 皮套。
据此皮套落地改为**三线并行**，并删除此前所有类似方案。

- **桥接新增 `VmcOscSink` 与 `--sink vmc`**（另有 `--vmc-host` / `--vmc-port`（默认 39540）/
  `--vmc-no-head` / `--vmc-vrm1-names`）。`--sink` 改为**支持逗号组合**（`vmc,unity`），
  别名 `both`=vts+unity、`all`=全部；**既有出口行为完全不变**。
  实现严格按规范最小集：`/VMC/Ext/OK`、`/VMC/Ext/T`、`/VMC/Ext/Blend/Val`×N、
  可选 `/VMC/Ext/Bone/Pos`（头部，角度为 0 时不发）、`/VMC/Ext/Blend/Apply`。
  **不打包 bundle** —— 规范里打包是可选行为，接收端本来就必须能处理未打包的消息，这样少一层编码风险。
- **表情名默认用 VRM0 预设**（`A` / `Joy` / `Blink_L` / `Blink_R`）：规范要求使用 VRM1 的发送端
  也**按 VRM0 格式发送**以兼容既有应用；`--vmc-vrm1-names` 可切到 `aa` / `happy` / `blinkLeft`。
  注意 `Blink_*` 语义相反（VMC 是"闭眼度"），映射时**取反**。
- **新增 `apps/vmc_link/`**：`monitor.py`（VMC 监听器，把 OSC 翻译成人话，**装 VSeeFace 前就能确认发送端**）、
  `selfcheck.py`（自检）、`osc_reader.py`（最小 OSC 解码器，含 bundle 解包）、`README.md`（操作手册）。

**已验证（本机实跑，不依赖摄像头/VSeeFace/Unity）**：OSC 编码**逐字节**核对 ——
`/VMC/Ext/Blend/Val("Blink_L", 0.5)` 编码为 36 字节且 hex 与规范一致；
取值方向（睁眼 0.2 → `Blink_L` 0.8、张嘴 0.6 → `A` 0.6）；**同一帧内** `Apply` 排在所有 `Val` 之后；
VRM0/VRM1 两套名；头部四元数归一化（模长 1.000000，偏航 90° → 绕 Y 轴 0.7071）；
`--vmc-no-head` 生效；`tests/` 56 个用例与 `apps/unity_link/selfcheck.py` 仍全通过。
**顺带修掉一处自检判据本身的错误**：原本跨帧比较 `Apply` 与 `Val` 的先后（第 1 帧的 `Apply` 自然在第 2 帧的
`Val` 之前），属于判据写错而非代码缺陷，已改为帧内判定。
**未验证**：与真机 VSeeFace 的联通、头部骨骼的轴向符号。

### 文档：三线方案取代 Unity 单线计划

- 新增 [`docs/plans/2026-09-15-皮套落地三线.md`](docs/plans/2026-09-15-皮套落地三线.md)：
  **速通线**（VMC → VSeeFace + VRM）/ **目标线**（Unity + UniVRM / Live2D）/ **保底线**；
  含三线共用前置（模型来源、Live2D 授权红线、感知层不动）、7 条验收口径（按线路区别对待）、
  合并排障表，以及与 `--sink` 架构 / 决策记录 / 失误记录 / 简历档位的衔接。
- **删除** `docs/plans/2026-09-15-皮套Unity落地.md`（Unity 单线，已被取代）。
  CHANGELOG 中早前条目引用的旧路径以新文件为准。README、MEMORY、简历稿的引用同步更新。
- 简历稿第五节：措辞档位改为**按线路区分**的 A′ / B′，并明确
  **"速通线结果不能套用目标线的验收标准"**（线 A 当前不发视线、无引擎侧工作）；
  第六节原有的"6 道坎"表**合并进三线方案**，只留简历语境下必记的三点，消除两处文档各说一套。
- `.dsh-dev/decisions.md` 新增三线条目，并**显式修正同日上一条"Unity 单路线"的表述**
  （不是那个决定错了，而是当时漏看了 VMC 这条现成标准）；VTS + PyQt5 仍然不做。

### 新增：`--input` 支持视频文件 —— 无摄像头也能跑通全链路（首个端到端实测）

`--input` 原先只接受摄像头序号（`type=int`），这意味着**整条链路必须有摄像头才能验证**，
P0 因此无法无人值守地跑。现改为接受**摄像头序号或视频文件路径**（视频放完 `cap.read()` 失败，
主循环自然退出），并给出可复现的验证命令：

```bash
python apps/vmc_link/monitor.py --port 39540          # 终端 A
python apps/unity_link/mock_receiver.py --port 39541  # 终端 B
python apps/vtube_bridge/main.py \
    --input 7f62f96bca5cffdfe2e0167bf3de3169.mp4 \
    --sink vmc,unity --vmc-port 39540 --unity-port 39541 --no-gui   # 终端 C
```

**这是本项目第一次把"感知层 → 出口"整条链路跑到底**，实测结果：

| 项 | 实测 |
|:---|:---|
| 视频 240 帧 | 全部处理完，桥接退出码 **0** |
| VMC 首帧表情 | `A=0.570` `Blink_L=0.011` `Blink_R=0.025` `Joy=0.000`；同帧 `EyeOpenLeft=0.989` → **取反正确** |
| VMC 附加消息 | `/VMC/Ext/Bone/Pos`、`/VMC/Ext/T` 均在发（头部角度非零时骨骼消息确实发出） |
| Unity 出口 | `seq` 1→238、`face_found=true`、**乱序 0**、参数逐帧变化 |
| **两出口一致性** | 同帧 `MouthOpen=0.570` ↔ VMC `A=0.570` —— 同一份感知结果，两个出口互相印证 |
| 发送率 | **21.4 帧/秒**（`--send-fps` 上限 30） |

> ⚠️ **新发现的口径问题**：实测发送率**只有 21.4 帧/秒**，不是设定的 30 —— 发送线程与主循环争 GIL，
> 实际速率被管线处理率限制（720×1280 约 47 ms/帧）。**任何"稳定 30 FPS"的说法都不成立**，
> 必须写成"设定上限 30、实测 21.4"。三线方案的验收标准第 6 条已按此订正。

**仍未验证**：接收端 —— 与真机 VSeeFace 的联通、Unity 侧 C# 的编译与运行。
即：**发送端这一侧已经不需要再验，剩下的全在接收端。**

### 新增：C# 桩编译静态检查（U3D 脚本首次"编译"过）

Unity 侧 3 个脚本此前一直标注"从未编译验证"，而本机没有 Unity。现补上用
**手写 UnityEngine 桩 + Visual Studio 自带 Roslyn `csc.exe`** 的静态检查：

- 新增 `apps/unity_link/CompileCheck/`：`UnityEngineStubs.cs`（最小 Unity API 桩）、
  `CompileCheck.csproj`（`dotnet build` 用）、`check.ps1`（自动找编译器：有 .NET SDK 就用
  `dotnet build`，否则用 VS 的 `csc.exe`；两者都没有时明确输出"仍属未编译验证"并跳过）。
- **语言版本对齐 C# 9**（Unity 2022.3 的上限），防止误用更新的语法。
- **结果：编译通过**（退出码 0）。当场抓到并修掉一个真实问题：
  `FaceParamReceiver` 里的 `rateWindowCount` 是**死字段**，已删除。
- **边界如实标注**：桩是手写的、与真实 Unity API 有差异，**"过桩"不等于"在 Unity 里能编译"**；
  首次导入仍以 Unity Console 为准。README 与三线方案均已写明这一限制。
- 顺带记录一个环境坑：**PowerShell 5.1 会把无 BOM 的 UTF-8 `.ps1` 按 GBK 解析**，
  导致中文注释把脚本解析坏（`Array index expression is missing`）；`check.ps1` 已保存为 **UTF-8 with BOM**。
- `.gitignore` 增加 `apps/unity_link/CompileCheck/bin/` 与 `obj/`。

至此三线里的**代码侧未知项只剩两处**：Unity 里的实际编译与运行、与真机 VSeeFace 的联通。

### 修复：UDP 出口的限频把管线帧率砍半（240 帧只发 120 帧）

端到端实测发现：视频 240 帧全部处理完，但两个 UDP 出口各只发出 **120 帧（20.0 帧/秒）**。

**根因（实测得出，非推测）**：管线帧间隔 **24.9 ms**（40.1 帧/秒）**小于**限频门限
**33.3 ms**（`--send-fps 30`），于是每一帧都"差一点点"、**隔一帧才发一次** —— 速率正好腰斩。
限频判据写的是"距上次发送是否够久"，在这种非整除关系下必然掉帧。

- **修复**：两个 UDP 出口**逐帧发送、不限频**。理由：UDP 是 fire-and-forget、没有背压，
  这个上限对它们只有害处。
  **实测结果：240 帧 → 发送 240 帧（40.2 帧/秒），皮套更新率翻倍**；VMC 共 1681 个 OSC 包（≈7 包/帧）。
- `--send-fps` 明确为**只对 VTS（WebSocket）出口生效**（那条路是有状态连接，接收端有自己的节奏），
  帮助文本已写明。
- 顺带简化：去掉两个 UDP 出口的发送线程（`sendto` 无握手、耗时几十微秒，内联即可），
  并在主循环结束时**新增 summary 打印**（处理帧数 / 管线帧率 / 各出口发送数）——
  下次遇到"慢了少了"不必再靠猜。

> **过程中的一次错误（已记入 `.dsh-dev/mistakes.md` 失误 3）**：我先**没有加计数器**就断言原因是
> "发送线程与主循环争 GIL"，并把这个结论写进了三处文档；基于该假设做的第一版修复
> （改成主循环内联发送）**完全没有效果**（120 → 120 帧）。加上计数器后才看到真实分布：
> 管线其实跑到 40.1 帧/秒，问题在限频判据而非线程模型。
> 四处文档的错误表述已全部订正，教训是：**有计数器就不要靠推理**。

### 新增：`apps/vmc_link/e2e_check.py` —— 一条命令跑完的端到端冒烟测试

不需要摄像头、不需要 VSeeFace、不需要 Unity：本进程起 UDP 监听 → 以子进程跑真实桥接
（输入是仓库自带的演示视频，放完自动退出）→ 按 VMC 规范解码收到的包并逐项校验
（消息齐全、类型正确、取值在 0..1、帧内 `Apply` 顺序、骨骼消息存在、帧数下限）。

**实测输出**：`[PASS]`，240 帧处理完、240 帧发出、40.2 帧/秒、1681 个 OSC 包。
用途：**在装 VSeeFace 之前先把发送端钉死** —— 这样如果皮套不动，问题一定在接收端配置，
不必来回怀疑。命令：`python apps/vmc_link/e2e_check.py`。

### 文档：补上可直接下载的 VRM 皮套来源（含 CC0 与官方示例）

此前只写了"用 VRoid Studio 自捏"，而速通线的第一步就卡在"没有模型"。现补上两条**已核实授权**的来源：

- **① Sendagaya_Shibu（首选，CC0）**：[BOOTH](https://booth.pm/ja/items/7421307) 免费下载（14.9 MB），
  上传者原文"全てCC0ライセンスのモデルですので、ご自由にお使いください"，并明确"**制限を追加しません**"（不追加限制）。
  CC0 = 免署名、可商用、可修改 —— 演示视频与仓库都能直接公开。
  同时给出 [VRoid Hub 上的 VRM0.x 原始版](https://hub.vroid.com/characters/4593660874193246717/models/1289835761881499317)
  （**VSeeFace 老版本对 VRM0.x 兼容更稳，先试 0.x**）。
- **② [Seed-san](https://github.com/vrm-c/vrm-specification/blob/master/samples/Seed-san/README.md)（VRM 官方示例）**：
  授权为 [VRM Public License 1.0](https://vrm.dev/en/licenses/1.0/index)，模型作者 VirtualCast, Inc.。
  逐字读过许可后确认：**Model Use（把动起来的形象做成画面给第三方看，演示视频属此列）是默认授予的**；
  但**附加条件写在模型文件自身的 License Settings 里**（是否要求署名、是否限非商用等），发布前应用 VRM 工具打开确认。

> ⚠️ **未核实项（已在文档中标注）**：VRoid 关于"旧 beta 版样本模型使用条件"的官方帮助页
> 本次被反爬拦截（403），因此"① 的原始授权也是 CC0"依据的是 BOOTH 上传者的声明，**不是 VRoid 官方页面原文**。
> 对使用者而言起作用的正是分发方声明的 CC0 授权。

### 决策改定：皮套形态改为 2D Live2D，VTS 路线重新启用为线 A

用户看到 3D 免费样本模型后反馈"**好像有点丑**"，要 2D 皮套。据此改定形态，并**重新启用此前被否掉的 VTS 路线**：

- **关键：代码零改动**。`VTubeStudioWorker` / `VTubeStudioClient` 是项目原有实现，
  而我们的参数名（`FaceAngleX/Y/Z`、`MouthOpen`、`EyeOpenLeft/Right`、`EyeLeftX/Y`…）
  **本来就是按 VTube Studio 的标准定的**。
- **"VTS 需要 PyQt5"这条否定理由已不成立**：`--no-gui` 让它能无窗口运行。
  **实测**：本机**没有装 PyQt5**，`--sink vts --no-gui` 仍正常启动并尝试连接 `ws://127.0.0.1:8001`
  （`Connecting to VTube Studio ...` → `[WARN] VTube Studio unavailable: timed out`，因 VTS 未安装，属预期）。
- **2D 反而多一项能力**：VTS 有标准视线参数（`EyeLeftX/Y` 等），我们的管线本来就输出眼球方向；
  而 VMC/3D 那条路当前**不发**视线 —— 验收口径已按线路区分。
- **VMC + VSeeFace 降为 3D 备选**，代码与手册（`apps/vmc_link/`）保留、不占线位。
- **新发现（决定要不要装 Cubism Editor）**：[Live2D 官方样本库](https://www.live2d.com/en/learn/sample/)
  的下载包内含 **runtime folder**（`.model3.json` + `.moc3` + 纹理 + 物理设置），
  **这正是 VTube Studio 能直接导入的格式 —— 不需要 Cubism Editor**。
  授权：个人 / 小规模企业（年销售额 < 1000 万日元）**可商用也可非商用**（Unity-chan 与初音未来除外），
  但**必须标注指定版权声明**，且 Hiyori / Miara **不得改动设计**。
- **新增操作手册** [`apps/vtube_bridge/README.md`](apps/vtube_bridge/README.md)：安装、插件 API、模型来源与授权、
  命令、7 条验收、排障、已知限制（含"2D 参数集因模型而异"这个与 VRM 的关键差异）。

**仍未验证**：VTS 真机联调（连接 → 授权 → 参数注入 → 皮套跟随）—— 本机未安装 VTube Studio。
三线方案总览、模型来源、验收说明与衔接表均已按本条更新。

### 文档：决策清单补入"小脸裁剪放大实验"与皮套形态两条

`docs/简历项目经历.md` 第九节「决策者必答」由 10 条扩为 12 条，新增：

- **用小规模 A/B 实验否决"小脸裁剪放大"方案**（`probe_aug_crop.py`）：Tiny 召回 +0.007、总体 +0.004，
  但精确率 **−0.043**、多出 1,881 个预测（约 **13:1**）—— 收益不显著而代价明确，不足以支撑数据集级改造；
  转向提高输入分辨率。这是"提出假设 → 最小实验 → 数据否决 → 转向更高杠杆方向"的完整闭环，
  也是简历上最值钱的一条决策证据。
- **皮套形态与授权红线**两条（见上）。
